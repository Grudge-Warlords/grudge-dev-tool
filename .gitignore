node_modules/
dist/
release/
.vite/
.cache/
*.log
.env
.env.local
.DS_Store
Thumbs.db
.history/
dev.venv/
Untitled-1.md

# Portable toolchain (Blender / ffmpeg / large zips) — install via npm run toolchain:install
tools/blender/
tools/ffmpeg/
tools/*.zip
tools/blender-mcp/**/*.zip
!tools/blender-mcp/.gitkeep
!tools/README.md

# Secrets — never commit these even if they slip into the working tree
secret.txt
secrets.txt
secret.*.txt
secrets.*.txt
*.secret.txt
*.secrets.txt
.secrets/
*.pem
*.key
classic64-dry-run.json
manifest-preview-*.json

# Database dumps (scripts/backup-postgres.mjs) — never commit player data
backups/

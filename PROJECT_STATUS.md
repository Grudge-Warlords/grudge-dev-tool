# Grudge Dev Tool — local status

Updated: 2026-09-07. Authoritative checkout for this work: `E:\grudge\grudge-dev-tool`, branch `main`.

## Release 1.1.1 — 2026-09-07

Owner authorized commit, merge as needed and deployment of the latest Dev Tool. The fixes are directly on `main`, already based on the current remote main; no unrelated feature branch is included. The patch release preserves selected local libraries through skeleton review and lets detailed character briefs, including unfamiliar humanoids carrying accessories, reach the existing HY-Motion semantic gate. Independent source review found both issues and confirmed their fixes.

Verified Windows release installer: `release/release-1.1.1-20260907-final/Grudge Dev Tool-Setup-1.1.1.exe`, 135,091,380 bytes, SHA-256 `81affd3ac87dd097530b5d61064afbeeac7eb397196b85ea4b5d95b735ea81be`. Its update manifest matches the installer size and SHA-512; the packaged GitHub feed identifies the existing Grudge-Warlords/grudge-dev-tool repository. Packaged `app.asar` SHA-256 is `8028f34826d0bb901327be8fdd29b7d8e31774c58591fd6285fae72f48810f5e`. Both TypeScript projects, rigging/HY-Motion/prompted-animation/workflow/renderer recovery checks, updater guards, full production build, icons, sandboxed preload and packaged startup passed. All 11 maintenance tests passed; the discovery observer correctly stopped at its dirty-tree baseline and made no repository changes.

The release package visibly loads the retained kangaroo in Skeleton Studio's Place step and restores its exact motion prompt, seed 119, duration 4 and replace mode; the CPU action is enabled. A separate pair of temporary library selection fixtures verified that the second library survives the same return; this did not generate or apply a library clip. Evidence and hashes are under `.cache/release-1.1.1-20260907/`. These checks do not claim new provider inference, installation over the user's running apps, or owner visual acceptance of animation; the September 6 evidence below is a historical snapshot.

Published [v1.1.1](https://github.com/Grudge-Warlords/grudge-dev-tool/releases/tag/v1.1.1) at `2026-09-07T01:50:54Z` from source commit `808990ea44fe93fd587a5a82d26c009faef07ac6`, pushed directly to `main` and tagged `v1.1.1`. GitHub's latest-release endpoint identifies this non-draft, non-prerelease release. All three assets (installer, blockmap and `latest.yml`) were downloaded again and their SHA-256 hashes exactly match the verified local artifacts. The tag-triggered packaging run `34074185583` was intentionally cancelled before build/publication so the manual release contains the locally verified installer; no other workflow was cancelled or disabled. Docs deployment and CodeQL for the release source succeeded. The temporary release-check app was closed; the previously running user apps and unrelated untracked review notes remain intact.

## Current: animation application and Skeleton Studio recovery — 2026-09-06

Al's two current CPU failures (`2fe7748a-f932-4cc0-a764-e1ff61f19baf`, `a951e6df-e7e5-48e8-ab24-b37de5b7d66e`) both requested kangaroo hopping with synchronised legs and tail balance. The old subject allowlist omitted kangaroo and stopped as ambiguous. A separate admin-only navigation flag redirected the local test session from `/skeleton` to `/studio`. Shared animal-body classification now reaches the existing creature morph/deformation author and explains the human-only HY-Motion boundary before provider work. Known incompatible animal motion is not sent to HY-Motion and is never labelled neural or skeletal.

Normal packaged UI controls created animation `860d63cc-0030-4715-b222-89574c67de90` from the already approved Paint source `2a9fd370-5ab8-4675-aa4a-0b93ba228f59`. The result is `E:\GrudgePrompt3D\workflow-history\860d63cc-0030-4715-b222-89574c67de90\asset.glb`: one four-second clip with five morph targets, three effective deformation operators (synchronous bound, rear counterbalance and squash/stretch), plus the prompted hop path. The exact geometry and texture fingerprints remain unchanged and the parent SHA-256 remains `e089e1ba041a41ca2002c6ebfce1ff73c855860f0f800a1ae100b69ea2d3d202`. The app reached animation review, played the complete clip and the full viewer showed body/paired-limb and rear-region changes across time-separated side-view frames. This is real CPU application/playback evidence, not anatomical recognition, generated HY-Motion, or owner visual acceptance; the new revision remains unapproved and was not saved as an approved final export.

The actual **Review fitted skeleton and place markers** control opened Skeleton Studio with the exact source model and **Place** selected in a non-admin local session. Source bytes are hash-verified and materials preserved. A retained-context round-trip check in the final v2 package restored the original prompt, seed 119, duration 4 and replace mode; the CPU action was enabled and the human-only HY-Motion choice disabled for this animal. Both corrected-marker return and return without markers preserve the source selection. Enabling local controls now restores finishing history immediately without a tab switch or stale progress overwrite.

Passed: deterministic rig/correction/library tests, HY-Motion binding and semantic-gate tests, prompted-animation tests, authoritative workflow identity/approval/save tests, renderer recovery checks (updated stale one/four-image labels), both TypeScript projects, production build, sandboxed preload, v1 and final v2 directory packaging and operational packaged smoke. Existing Windows icon generation/coverage also passed the build. Final executable: `E:\grudge\grudge-dev-tool\release\animation-repair-20260906-v2\win-unpacked\Grudge Dev Tool.exe`; SHA-256 `9c7cb03812e3ecbf84b2dc0e7e8eef8e148986b32f43e44a5b4f6ae282d4c50f`. Final `app.asar` SHA-256 `67ac74f989aadd512810b666216547eee1a977f0dae9962fa90f0518014a3e48`. Detailed checks, screenshots and reports are in `.cache/animation-repair-20260906/`. Final v2 is open as PID 20824 using the task profile `.cache/animation-repair-20260906/profile-v2` and retained storage `E:\GrudgePrompt3D`, with no remote-debugging listener. Task-owned v1/v2 diagnostic processes were closed; the previously running v8 user app was preserved. Source is uncommitted on the existing `main`; unrelated untracked review plans are preserved. No provider inference, cloud fallback, installation over the existing app, push, publish, or unrelated workload cancellation occurred.

## Current: guarded local creation, neural workflow and editable regions — 2026-09-06

Six primitives and existing local/catalog GLBs enter the creation service without requiring Hunyuan. Reused files become immutable-source working copies retaining source identity, embedded metadata, materials, skin and clips. The main page keeps one subject prompt as the primary input; optional planning, local settings and history remain collapsed. Save/reopen, bounded resizing, local materials and animation all use the same retained-revision path.

The normal packaged app passed create, adjust, animate, save and reopen checks with a box and a real GrudgeBlox hammer. A genuine local Toon RTS Orc retained 472 triangles, 17 bones and 53 clips, played its walk clip, resized without losing its rig/clips, and reopened from managed storage. The public catalog path was also exercised through its visible **Use as base** controls: `asset-packs/toon-rts-characters/glb/characters/orc.glb` loaded as a self-contained 4,104-triangle working copy with one embedded texture and 20 bones. Animation-only catalog entries do not offer that model action. Camera presets now fit actual projected bounds so long and tall models remain large enough to inspect without clipping.

The animation screen exposes actual influence previews and up to four named/coloured custom ellipsoids with selection, bounded model-relative commands, strength, coverage, undo/reset and sibling saves. Focused checks cover 50%/150% strength, stable timing and geometry/material fingerprints, custom-setting persistence, invalid/ambiguous input rejection and second-clip isolation. This proves the implementation contract; a real neural region edit was not run because the new kangaroo's textures were correctly rejected first.

A real one-image Hunyuan kangaroo run produced 50,000-triangle geometry. Front, right, back and left views were inspected in the packaged app and the complete body, ears, feet and long tail were accepted for that exact geometry revision. Two real Hunyuan Paint attempts were inspected from four sides and rejected: both contain incoherent black/high-contrast patches across the head and upper body, while the first also contains blue/orange artifacts and the second leaves much of the tail white. Both reference images are coherent kangaroos, while the painter's base-colour JPEGs already contain the same mosaic artifacts before the app's geometry-preserving merge. The runs selected application-defined 256 px Paint even though upstream supports 512/768 and recommends at least 21 GiB VRAM for six views at 512. That unsafe reduced profile is removed: new Paint work uses a new immutable `hunyuan-paint-official-512-v1` identity, upstream 512 px generation and 2048 px rendering, and fails as busy below 21 GiB free rather than producing another reduced-quality result. Neural animation, final save and overall chain acceptance remain blocked until a new texture passes visual review; technical completion is not being reported as semantic acceptance.

A separate prompt-only run produced a complete provider kangaroo, but v7 stopped before geometry because its closest foreground pixel was only 9 px from an edge. The corrected preparation rule still rejects any mask touching an image boundary, while safely centring a close but fully isolated provider image and retaining the untouched source/hash. Applying that exact deterministic repair to the retained provider image moved the minimum working margin from 9 px to 216 px and still requires human semantic approval. One-image and prompt-only modes therefore have real provider evidence. The four separately labelled input controls, byte-duplicate rejection, stable role order and provider binding pass focused checks, but four supplied images have not yet reached a real Hunyuan inference run.

Output-space preflight now runs before provider verification/inference and reports the exact selected root, measured free space and conservative stage allowance: 0.50 GiB for concept, 3.00 GiB for geometry and 4.00 GiB for Paint. The live acceptance root moved to `E:\GrudgePrompt3D-Acceptance-Gen5`. A prior geometry job on the full D: volume failed during final export; its partial 14,281,616-byte GLB was copied to E: and hash-verified. Automatic approval review rejected deletion of the redundant D: copy, so D: remains full and no broader cleanup was attempted.

The final v8 package passed a full build, sandboxed preload check and isolated packaged operational smoke. Executable: `release\non-hunyuan-workflow-20260906-v8\win-unpacked\Grudge Dev Tool.exe`, 223,309,824 bytes, SHA-256 `4F61C1F774A805AADBA542615E51DE92E834EFE33746AED81478EFAA09871106`. Packaged `resources\app.asar`: 77,479,357 bytes, SHA-256 `AC514070C2A7F46351A97F7C239A85615A72503E4F6A62FBB43F6D853590A3BD`. It is running visibly for manual testing as PID 19216 without a remote-debugging listener, using the retained isolated E: workspace. The scoped local commit containing this status is recorded in git history. Al's existing Dev Tool PID 32244 and History Engine PID 60024 remain untouched. Native computer control is still unavailable with `failed to write kernel assets: The system cannot find the path specified. (os error 3)`; all acceptance interaction used the task-owned isolated Electron window and normal visible controls. No push, deployment, original approval change or unrelated workload cancellation occurred.

See `.agents/plans/non-hunyuan-workflow/` and `D:/gruda-build/outputs/non-hunyuan-workflow-20260905/` for the scoped baseline, review and retained checks.

## Current: guided reliability audit and reviewed Windows installer — 2026-09-05

The active creation workspace now shows one primary subject prompt with optional settings and serial batch collapsed. Raw numeric edits remain visible and invalid values are blocked in both renderer and main-process admission. A single explicit comprehensive concept-review attestation retains the existing three exact pass conditions; individual checks and all later visual gates remain available. Known unsupported motion transformations are rejected before a supported spin can conceal them. Contrasting clauses retain affirmative intent while temporal `not yet` remains negative.

Managed-save errors take priority over a previous catalog record, with one automatic attempt, duplicate-operation protection and explicit Retry save. Provider readiness ignores stale replies and refreshes the current provider after installation events. Fleet probes have eight-second abort deadlines, release unused bodies and reject API/CDN HTML fallback pages. CLI doctor recognizes the canonical project and returns failure consistently in JSON/text when login is missing. Updater state survives renderer reload; downloads and installs remain explicit, deduplicated and restricted to the trusted main frame. Event-reported install failures release the operation latch.

Passed: both TypeScript projects and CLI checks; focused input, real local HTTP timeout/fallback, motion, workflow, recovery, orchestration, exact approval, batch, reference, visual-inspection and artifact checks; eleven maintenance-policy checks; production renderer/main/preload build; installer packaging and isolated packaged operational smoke. Actual keyboard input proved invalid seed rejection and correction. Independent reviews found and resolved readiness, negation, updater reload and saved-copy feedback defects. See [the workflow contract](docs/guided-workflow-reliability.md) and `D:\gruda-build\outputs\guided-reliability-20260905\AUDIT.md` for scope and evidence.

Final installer: `release\guided-reliability-20260905-final\Grudge Dev Tool-Setup-1.1.0.exe`, SHA-256 `297114C3B72BA8D73284B5A174F68E08A61FD91787942632E0995BD1C2D74BDE`. Packaged `app.asar`: `818BB8EBEA574D4B6D64DEE02B32186C9A0D021E6F13FD3A08990AF4C51F7D61`. This supersedes the earlier v3 and intermediate audit packages. It is not installed over the normal running v2 app; owner profile and processes were preserved.

Outstanding: actual Hunyuan visual chains remain **0/6 individual and 0/6 fresh serial**; the crystal's flower-opening motion and final save/export/reopen remain unaccepted. No new provider inference, model install or cloud substitution occurred. CLI login is absent. Maintenance budgets, authority and schedule remain intentionally unapproved/off. No commit, push, deployment or release publication occurred.

## Current: installed readiness update and real crystal workflow — 2026-09-05

The saved `main` checkout was refreshed against origin (ahead 2, behind 0), preserving all pre-existing dirty work. Guided new-asset creation now starts from clean category, scale, seed and object defaults instead of inheriting the previous creature. Existing-revision edits retain their intended settings. Affirmative prop-motion wording, including spinning and floating, now includes animation in the guided route; negated motion does not. Local packages without a configured update feed skip update checks rather than displaying a misleading missing-configuration error. No update feed or provider download was invented.

Passed: focused new-root/routing regressions, both TypeScript projects, production renderer/main/preload builds, sandboxed preload verification, and packaged operational smoke. The Windows branded icon set, installer icon and file-association icon are covered. The v2 installer replaced the older installed application at `C:\Users\mjneu\AppData\Local\Programs\Grudge Dev Tool`; its installed `app.asar` matches the package at SHA-256 `B7CA9BBAA819199897AF545F205267779A85B4CA77923C4401939C740763C807`. The original app and profile are recoverable under `E:\GrudgePrompt3D\devtool-runtime\rollback\20260905-pre-update`. The running app retained the normal signed-in profile and reached the live fleet status surface.

The final v3 package adds the local-no-feed updater correction and has passed build and packaged smoke, but installation and normal-restart verification remain pending while the owner is using the running app. Installer: `release\ready-for-use-20260905-v3\installer\Grudge Dev Tool-Setup-1.1.0.exe`, SHA-256 `9B2C16E4017E545A3B113FE03FE75697CBE0D2527441AA229AC8082BE21CA319`; packaged `app.asar`: `781BD4A7D85E04493DB8199D3DC2AE766D66BF408456ECCFFDFC54236B6DF505`.

A fresh real Hunyuan crystal prop was generated and painted on the existing RTX 3090 without replacing models or stopping unrelated workloads. Root job `cf3dbc1e-bd89-4116-bdda-e48364207f14` retained the signed provider verification, accepted isolated concept, 50,000-triangle one-metre-tall geometry and four-view geometry approval. Paint job `c0fb7c84-aeba-4e32-b181-0b5e7cfc6a19` used the balanced six-view Hunyuan Paint profile and produced a self-contained violet/turquoise material result with unchanged geometry hash `786059f0ba868fcf8023b78ceb556d69f5af1c0540cbb6cc7d3f7c40ae77db0f`. Four texture views were inspected and the app retains exact texture approval. Painted GLB SHA-256: `79c057e809ba19a6cb13c879c80bbd37d693256e7366d0d85b588323b3b62fe6`.

During the shared live run the motion prompt changed from the planned slow spin to “centrally spinning, top of crystal spreads out like a flower with 6 petals then folds back into place.” Revision `f5b2958c-7535-4ce6-9f51-7565ab140664` completed structural checks but contains only rigid spin and 1.25 m linear travel, no morph targets or joints. It therefore does not establish the requested flower-opening behavior and is not accepted by this task. Final animation acceptance, managed save/export and reopened playback remain incomplete. The crystal is a thin shard, not a volumetric six-petal flower; successful technical validation is not semantic acceptance.

Evidence boundary: the genuine generation/paint route and the installed v2 startup are verified. This does not establish every optional provider or a complete end-to-end accepted asset yet. HY-Motion still needs its signed installation manifest and TRELLIS needs its native artifact; neither was used. Deterministic prop animation is not HY-Motion. The earlier strict acceptance remains **0/6 individual and 0/6 fresh serial-batch chains**. No commit, push, deployment or daily-maintenance schedule activation occurred.

## Current: capability-based Hunyuan generation and live semantic gate — 2026-09-05

Hunyuan generation now selects an operation-specific profile from measured capacity and current headroom instead of requiring a named graphics card. Geometry can use full GPU (`30` Hunyuan concept steps, `50` shape steps, `512` octree), lightweight GPU with native CPU offload (`30`/`30`, `256` octree), or genuine CPU inference (`20`/`20`, `256` octree) when at least 64 GiB total and 32 GiB free system RAM is available. Hunyuan Paint selects the same pinned provider at either balanced six-view `384`/`2048` or lightweight six-view `256`/`1024` settings. Selected profile, device, exact settings and GPU UUID when applicable are retained and revalidated through job, result, provenance and lineage. No procedural mesh, stock model, cloud call or unlabelled provider substitution is introduced.

The shared prompt compiler now retains generic creature anatomy and spatial relationships, preserves explicit counts, keeps the subject before presentation clauses within Hunyuan's conditioning budget, and uses a concise natural-language path for short creature briefs. Default presentation remains one isolated subject on plain white with no stand, text, scenery or unrelated object. Negative parsing no longer turns “no separate fruit” into a positive-looking `fruit` token, and broad phrases that can suppress creature-body generation have been removed. This is generic relationship handling; it contains no Kiwi Dog or six-acceptance-asset implementation.

A real offline Hunyuan run accepted concept job `16019598-14fe-4df8-8e02-4c9079ff17ef` at seed `9031285`, then generated a new 3,601,416-byte GLB with SHA-256 `9c75309cc1de8d23891f2a4785ae98140b26b8263cb8aa25626b7989acd7110b`, geometry hash `7d143bd9ac9133f9b978359acd1bef557f118da65e9165404b803e08fad1d49d` and 49,998 triangles. Technical validation passed. Six-view inspection found a clear oval fruit-bodied dog, distinct dog head, floppy ears and four legs, but no clearly visible required rear tail. The geometry is therefore visually rejected and the workflow has not advanced to Paint, motion, save/export or reopen. Lightweight Paint remains source- and contract-tested but deliberately was not runtime-tested before geometry acceptance.

Passed after the final correction: production renderer/main/preload build; both TypeScript projects; Prompt-to-3D provider/profile/contract checks; prompt-retention and negation regressions; and Python syntax for the provider worker and bounded diagnostic sweep. No new package or application relaunch was performed at session close, and no generated asset was represented as accepted on technical checks alone. The original strict acceptance state remains **0/6 complete individual chains and 0/6 complete fresh serial-batch chains**.

Resume gate: reopen job `16019598-14fe-4df8-8e02-4c9079ff17ef`, submit one genuine prompted Hunyuan shape refinement that adds a clearly visible curled rear tail while retaining the accepted oval body, dog head, two floppy ears and four legs, then render and inspect the revised geometry from multiple views. Only after visual geometry approval should the lightweight Hunyuan Paint profile be exercised as the intentionally last fallback test.

## Current: governed daily maintenance control plane — 2026-09-05

The source now contains a fail-closed daily maintenance observer, exact scheduled-task prompt, digest-bound owner policy, external run ledger, immutable per-run JSON/Markdown/SPDX artifacts, a persistent capped-candidate queue, lease protection, local source/provenance/icon/lockfile/disk/GPU inspection, metered opt-in registry and OSV discovery, candidate scoring, separate evidence classes, isolated-staging rules, quarantine/hold/rollback requirements, and explicit always-approval boundaries. `ci:local` is separated from live fleet checks, machine-readable doctor runs are read-only, the Worker audit no longer assumes an obsolete Dev Tool clone, update discovery no longer implies download/install, and publish dry-run no longer fetches or rebases.

Safety state: every numeric budget remains `null`, every authority switch is off, policy approval is absent, and schedule creation is intentionally not performed. Manual observer runs can write maintenance evidence only outside the repository. Source mutation, scheduled execution, maintenance validation, network discovery, install, model/provider changes, process termination, staging, promotion, commit, push, deployment, release, and retail-app restart remain disabled or separately approval-bound.

Passed: eleven focused maintenance policy/SBOM/source-seal/candidate-queue/cap tests; the consolidated source-only `ci:local` path; both TypeScript projects; a fresh sandboxed preload build/test; the full production renderer/main/preload build; JSON/syntax/diff checks; and three isolated fresh-eyes review/fix rounds followed by a same-context final audit. Real manual observer run `2026-09-05T02-00-11-545Z-e7838f0d` completed with zero network bytes, no remaining lease, an intact external candidate queue, a closed mutation gate, correctly unestablished source evidence for the pre-existing dirty tree, and a 783-package SPDX 2.3 inventory whose root license is `NONE` and scoped npm package URLs preserve their namespace separator. Its immutable record, summary, and SBOM are under `C:\Users\mjneu\AppData\Local\Grudge Dev Tool\maintenance\runs\2026-09-05T02-00-11-545Z-e7838f0d`.

Evidence boundary: this establishes the authored source behavior and build integration. It does not establish a new packaged artifact, packaged runtime, updater service response, provider behavior, deployment, retail-device adoption, or owner acceptance. No dependency metadata/advisory network scan, package, install, app restart, process stop, commit, push, release, deployment, or schedule activation ran.

## Current: unified quality-first routing and GPU-independent animation — 2026-09-04

Prompt-to-3D now has one typed owner surface for new creation and exact-revision continuation. Its deterministic allowlisted router recommends existing methods in quality/capability order, then applies verified readiness, provenance/compliance and current resource feasibility. A blocked quality leader and next eligible route are shown before continuation; an explicit override is retained beside the automatic recommendation, enabled stages, exact revision and planner policy in original-creation requests or the neural `AssetSpec`. Hunyuan and TRELLIS remain explicit creation choices. Existing Hunyuan Paint/refinement, Skeleton Studio, local Forge, Scene Completion, validation/save and local animation services are routed rather than duplicated. No external LLM is eligible or contacted automatically. Optional schema planning remains an explicit loopback action using the existing `grudge-dev` preference, with no service start, model pull/install, `localAgent.ts` use or silent fallback.

The manual animation default no longer depends on HY-Motion readiness or a GPU/headroom probe. An exact approved painted GLB with a compatible canonical skin reuses it. An affirmatively identified, safely posed humanoid receives a deterministic CPU Mixamo-25 core fit with complete normalized four-weight coverage and finite inverse-bind matrices. Unsafe pose/limb evidence returns suggested markers to Skeleton Studio, and the correction is accepted only for the exact painted-parent SHA-256. An explicitly selected installed local Mixamo-25 library retargets its clip while the prompt compiler remains authoritative for root placement. Non-humanoid characters use the existing measurable morph/deformation route; props use explicitly labelled object-space motion. HY-Motion remains a separate optional provider and cannot be silently substituted in either direction. Shared negation-aware analysis now governs fallback anatomy, stance and skeletal-action terms: negated anatomy or motion is not treated as affirmative, mixed humanoid/non-humanoid body plans fail closed, and an unavailable optional Skeleton Studio library inventory no longer prevents the retained creation workspace from opening.

Focused evidence passed: the typed router's quality, blocked-leader, explicit-override, exact-revision and no-contact contracts; explicit TRELLIS retention/Forge routing without silent Hunyuan Paint; deterministic rig classification, immutable sibling creation, 22-joint mapping, full normalized skin coverage, finite inverse binds, geometry/texture retention, Skeleton Studio correction, deterministic skeletal clips, negated anatomy/stance/gait/flap/action handling, in-place versus directional travel, and local-library retargeting. The full Prompt-to-3D TypeScript/preload/provider/approval/reference/rejection/batch/workflow/Paint/visual/artifact/animation/viewport/sidecar/magic-byte regression set passed, as did authored-material fidelity and the production renderer/main/preload build. Image/reference functionality was deliberately tested last: local intake, single/multiview roles and hashes, tamper rejection, canvas selection, and concept crop/detached-shadow regressions passed. With Al's explicit authorization, the last check ran in the isolated disposable Python 3.11.9 environment `%TEMP%\grudge-concept-cv2-validation-py311` with OpenCV 5.0.0, Pillow 12.3.0 and NumPy 2.4.6; it did not alter system Python, a provider environment or the retail app.

The earlier `prompt3d-cpu-router-20260904-v2` package was superseded after the final multi-stage and TRELLIS evidence-boundary corrections. The final v3 isolated Windows package passes operational smoke with retained concept recovery, the exact Install Options/creation/preview panel set, new-asset image-source transition and prompt/settings transition without starting geometry. Package: `release\prompt3d-cpu-router-20260904-v3\win-unpacked\Grudge Dev Tool.exe`; executable length `223309824`, SHA-256 `687EBA36BC5C85CE5330B94DB9CC7EC123E1F781A33126F9539F4F5A2501C4FB`; `app.asar` length `77199747`, SHA-256 `73113ECA9948866FF9D0E3CA84EE1504FE4F7EFC0AADBD008C782AA2CD952980`; packaged HY-Motion worker SHA-256 `C1FE5DE48CCE112F4C16854EB5D1E7D114989165357EF04AF08786402525CDC3`; packaged Hunyuan worker SHA-256 `EB33043D01A1D7565E1359EC0CE8FD3016CC017807299F943F9B330DEC162E33`. Smoke used and removed a disposable profile; it did not install, terminate or replace the retail app or earlier manual packages.

Evidence boundary: this proves source, focused GLB behavior, build, package integration and isolated packaged startup. It does not prove semantic correctness on a real painted Hunyuan humanoid, visual rig/deformation quality, production engine use, deployment, or user acceptance. No genuine HY-Motion inference, provider installation, model download or cloud call ran. The basic CPU/library routes can produce labelled local assets but are intentionally rejected by strict Hunyuan acceptance. The original six asset chains and fresh six-item serial rerun therefore remain incomplete pending a compatible genuine non-humanoid prompted-motion provider and visual acceptance. The current UI is intentionally a minimal reversible scaffold; the final 4K layout, spacing, information hierarchy and screen-specific design remain pending Al's live design session and are not visually accepted.

## Current: capability-based HY-Motion hardware fallback — 2026-09-04

HY-Motion no longer requires a named GPU or treats display adapter/index 0 as the compute device. Readiness enumerates all NVIDIA adapters, retains their UUIDs and selects an ordered provider-native execution profile from fresh total/free VRAM and system-RAM measurements. The preferred `hy-motion-cuda-standard-v1` profile requires 24 GiB total/20 GiB free VRAM and binds `CUDA_VISIBLE_DEVICES` to the measured UUID. When that headroom is unavailable, `hy-motion-cpu-basic-v1` is allowed only with at least 32 GiB total/24 GiB free RAM; it invokes official HY-Motion `force_cpu`, hides CUDA from that worker and retains a reduced 12-step schedule. Both profiles load the same pinned official Lite checkpoint and emit genuine skeletal motion. There is no procedural, canned, rigid-transform or cloud substitute.

Execution profile, device, validation steps and optional GPU UUID are bound into the request, provider result, provenance and GLB binder. A mismatch fails closed. Install Options and the animation stage show the automatically selected profile and its tradeoff. A CPU result remains subject to the same compatibility, semantic, texture/geometry-retention and separate visual-approval gates; “fallback” does not mean accepted or production quality.

Live hardware proof found the RTX 3090 by UUID `GPU-87dda6d3-fc39-b7ea-8e1c-3360293f4506` with 24.0 GiB total and about 15.0 GiB free, plus 95.7 GiB total/about 57.7 GiB free system RAM. The app correctly selected CPU basic instead of falsely marking CUDA ready. Synthetic dual-adapter coverage proves that a capable RTX 3090 at index 1 is selected by UUID when a 6 GiB display adapter occupies index 0; CPU-only and insufficient-RAM failure cases also pass.

Passed: Python worker syntax; HY-Motion skin/binder/profile integrity; provider readiness and multi-GPU/CPU selection; all Prompt-to-3D provider, approval, workflow, batch, Hunyuan Paint, animation, visual, recovery, sidecar, preload and magic-byte regressions; final single/multiview reference-image tests; both TypeScript projects; production build; Windows directory package; packaged operational smoke; and packaged worker-presence/hash checks. Package: `release\prompt3d-workflow-20260904-64\win-unpacked\Grudge Dev Tool.exe`; executable SHA-256 `0A98C3B678321860D5DB084EFB696ECD7C53DC44667FBCD15DDF0A18B83A99F8`; `app.asar` SHA-256 `9F6421FB0CE8461AE3037D66C248C2F0D5B015DB15B16FA6D8DF9C67B2CC94D9`; packaged HY-Motion worker SHA-256 `C1FE5DE48CCE112F4C16854EB5D1E7D114989165357EF04AF08786402525CDC3`. The v64 manual build is responsive as PID 51152 with isolated profile `E:\GrudgePrompt3D\acceptance-profile-2026-09-04-v18`; retail, v62 and v63 processes were left untouched.

Evidence boundary: this proves capability-based selection, source/build/package integration and deterministic skeletal binding against signed motion fixtures. HY-Motion is not installed in the selected generator root, so neither CUDA nor CPU inference has run and no generated motion has received visual acceptance. CPU fallback retains the full model download and may be very slow; it is a lower-compute runtime profile, not a small replacement model. Official HY-Motion remains human/humanoid-only, so it cannot truthfully satisfy the six required asteroid, flail, rabbit, snake, fish and bird animation chains. Those original six workflows and the fresh six-asset serial batch remain incomplete pending a genuine compatible non-humanoid prompted-motion provider and visual acceptance.

## Current: local HY-Motion skeletal generation and Mixamo-25 binding — 2026-09-04

Prompt-to-3D now treats skeletal motion as a separately installed, provider-bound stage. The existing Studio Mixamo-25 skeleton remains the sole runtime bone contract: approved Hunyuan geometry with no skin is generically bound to the compatible 22-joint HY-Motion body hierarchy, while a complete retained HY rig is reused and foreign or partial rigs fail closed. Official HY-Motion rotation and root-translation samples are converted into GLB skin and animation channels; append and replace modes retain the approved geometry and texture hashes. Non-directional action strips horizontal root travel but retains generated body motion, and root travel is permitted only after a schema-constrained local prompt decision finds an affirmative direction. Negated, ambiguous, non-humanoid and unsupported multi-subject prompts are rejected rather than becoming an immediate programmed translation.

The installed-neural animation path no longer calls the earlier rule-based motion compiler. Its signed worker is pinned to the official HY-Motion source and model revisions, runs offline through an isolated WSL runtime, records the native motion-data hash and provider evidence, and validates all 22 required joints, active animated bone channels, skin coverage, normalized weights, geometry retention and texture retention before review. The creation UI accepts only prompt, duration, clip mode and seed for animation, retains explicit approval and rejection, and will not enable generation until both Hunyuan geometry and HY-Motion providers are installed and runnable.

Passed: the new synthetic fixture covers in-place skeletal movement, directional root travel, append mode, exact provider pins, geometry/texture retention, 22-joint skinning and compatibility rejection; all focused Prompt-to-3D workflow, provider, reference-image, approval, artifact, batch, Paint, animation, recovery, visual-inspection and sidecar checks; both TypeScript projects; Python syntax; production build; Windows directory packaging; and packaged operational smoke. Package: `release\prompt3d-workflow-20260904-63\win-unpacked\Grudge Dev Tool.exe`; executable SHA-256 `2333B0C196672AF9D893C4A80D158186AD5C6A6BBDD84282A00D9439566450B5`; `app.asar` SHA-256 `51A1DBD945A5E4BB55B1CF05B85BD7EB7B86F2F9C5BD79EF90E75842A0FB1093`. It is responsive as isolated manual-test PID 41032 with profile `E:\GrudgePrompt3D\acceptance-profile-2026-09-04-v17`; the installed retail app and the earlier v62 manual package remain untouched.

Evidence boundary: the v63 run proved source integration, exact-pin validation, packaging and deterministic GLB binding with signed fixture motion. HY-Motion was not installed and no genuine local HY-Motion inference or resulting visual acceptance was claimed. Its fixed CUDA-only gate has since been superseded by the capability profiles recorded above. Official HY-Motion is human-only; the six required asteroid, flail, rabbit, snake, fish and bird motions therefore cannot truthfully pass through it. UniRig was assessed as a possible generic auto-rigger for diverse geometry, but it does not generate prompted motion and was not added as a competing bone contract. Those six acceptance chains remain incomplete until a genuine compatible non-humanoid prompted-motion provider exists and its outputs pass visual inspection.

## Current: clause-bound motion analysis and synchronized creature action — 2026-09-04

The retained manual failure is reproduced exactly: “walking while moving forward with feet moving together to bending to a 45 degree angle and tail used for balance” previously became the same `linear + grounded-stride` plan as materially different prompts, discarded the synchronization, angle and tail requirements, and translated the 1.6 m subject 4.8 m. The new compiler retains five separate requirements and binds them to `path:linear`, `deformation:synchronous-bound`, `coordination:together`, `constraint:articulation-angle:45.000` and `deformation:rear-counterbalance`. The installed loopback Ollama model acknowledged all five through a schema-constrained CPU request before authoring. Missing, changed, conflicting or out-of-range clauses fail closed.

The GLB author now creates paired reach/tuck targets, counter-phase rear motion, gait rise and body lean. Unspecified travel for the exact 0.8 × 1.8 × 1.6 m subject is 2.0 m. A supplied front reference follows the retained +Z contract instead of being silently remapped to the prompt-only viewer-right presentation assumption. Nine fixed frames were rendered from the exact 13.3 MB retained Hunyuan source at `E:\GrudgePrompt3D\diagnostics\motion-v62b\kangaroo-motion-contact-sheet.png`; inspection shows visible paired lower-region movement, body rise/lean and rear counterbalance instead of the prior rigid slide. This is visual inspection of the corrected author, not user acceptance of a final workflow result and not a claim of skeletal or HY-Motion output.

Passed: the exact-prompt compiler/author regression, semantic acknowledgement and omission gates, both TypeScript projects, production renderer/main/preload build, workflow approval/lineage/save, renderer recovery, strict batch approval/acceptance, artifact issuance, reference intake, rejected refinement, Paint retention, motion trail, Forge animation, viewport navigation, sidecar, preload and magic-byte checks. The fresh directory package passed operational startup smoke. Package: `release\prompt3d-workflow-20260904-62\win-unpacked\Grudge Dev Tool.exe`; executable SHA-256 `8B527DF008F7CA14FAA21879D47C2E8D039571CB61FC6652E60B44F8CF2F5E58`; `app.asar` SHA-256 `AFAE66D2C6A03BA6BEB33089AB78F19CEE40C9C0E93C7FF93D1DD9147A228F8E`. It is responsive as PID 47112 on the retained manual profile. The v61 test processes were closed; the separately installed retail application remains running and untouched. No new manual workflow animation was submitted or approved after relaunch.

## Current: animation prompts drive body mutation — 2026-09-04

Animation intent now scopes body-part wording separately from whole-subject placement. The retained failure prompt — “body should bend down … legs/feet move forward … returns to the original position” — now compiles to an in-place right-facing creature plan with continuously weighted grounded limb motion plus crouch/bend deformation, not a downward 4.8 m root slide. Whole-subject return requests author a closed out-and-back path. Creature/character prompts that only translate or rotate the root are blocked in the visible editor and in synchronous backend preflight. Post-serialization validation now measures each morph target's maximum vertex displacement and animated weight; zero or inactive mutations fail before review. The exact corrected request was authored against its retained 13.3 MB Hunyuan source, with zero root displacement, active grounded-stride ratio 0.0259 and active crouch ratio 0.0898, then rendered across multiple frames to check that the body/feet move and return without the old hard-region mesh tearing. Type checking, production build, packaged smoke, the full focused Prompt-to-3D workflow/batch/approval/reference/animation/artifact/renderer set, Forge/trail/viewport checks, and concept preparation in the installed WSL Hunyuan runtime pass. Package: `release\prompt3d-workflow-20260904-61\win-unpacked\Grudge Dev Tool.exe`; `app.asar` SHA-256 `63D7B19CC28C6A177C6E64087355532E30A578A2E78651B7A53313993F49E938`. It is running as PID 54064 on `E:\GrudgePrompt3D\acceptance-profile-2026-09-03-v16` for manual testing; the installed retail application remains running and untouched.

## Current: Stage 4 can retain approved base geometry — 2026-09-04

After the exact base Hunyuan model is technically valid and has completed explicit multi-view geometry approval, Stage 4 now offers **Use the existing generated model** alongside the optional shape-refinement prompt. Choosing it advances to texture configuration without generating a replacement mesh. The decision survives a renderer restart and is retained in the sealed workflow lineage with the exact generation job, first variant, model hash, geometry hash and visual-approval hash. Texture approval, animation, managed save, portable export and reopen revalidate the same decision. The action is unavailable during strict serial acceptance; all six strict batch items still require a fresh prompted and approved Hunyuan shape refinement. Type checking, production build, the complete focused Prompt-to-3D/batch/artifact/animation regression set and the packaged operational smoke test pass. Package: `release\prompt3d-workflow-20260904-60\win-unpacked\Grudge Dev Tool.exe`; `app.asar` SHA-256 `CDE365BF6424A1B7307D6A6DB16361D70BEA5B7CC5400B38087065F3EAD783A8`. It is running as PID 37340 on the retained isolated acceptance profile for manual testing; the installed retail application remains running and untouched.

## Current: image source is the first new-asset stage — 2026-09-04

Every new Prompt-to-3D asset now begins at Stage 1 of 9 with two large actions: **Generate image** and **Select image**. Generate image clears reference evidence and opens the prompt/dimensions/settings stage for local HunyuanDiT concept generation. Select image immediately opens the existing bounded one-to-four-file picker and proceeds only after a valid selection; cancelling leaves the user at Stage 1. The selected method is shown at Stage 2, can be changed before generation, and is enforced by the generation readiness gate so stale reference inputs cannot silently cross between the two routes. The isolated v59 package passed an actual renderer transition smoke check from a retained concept into **Start new asset**, Stage 1, and then **Generate image** into Stage 2 with prompt and generation settings visible. Package: `release\prompt3d-workflow-20260904-59\win-unpacked\Grudge Dev Tool.exe`; `app.asar` SHA-256 `B4161D9E41C52F983BFC1702CB47DF83538CE03DCF689B2966A3F679FF50D695`.

## Current: Hunyuan reference-image generation — 2026-09-04

The Prompt-to-3D creation stage now accepts one to four local PNG, JPEG or WebP images. The initial file order is front, left, back, right; every role remains visible and can be corrected before generation. One image continues through the installed Hunyuan3D 2.1 image-conditioned shape path, while two to four labelled views select the separately pinned official `tencent/Hunyuan3D-2mv` checkpoint. The existing prompt-only path remains the default, and the strict six-asset acceptance batch remains prompt-only.

Each selected image is decoded and bounded before use, copied into the new job before provider preflight, SHA-256 bound to the exact AssetSpec and concept approval, and reverified at provider dispatch, save, export and reopen. The provider passes only the verified front/left/back/right map through the official Hunyuan multiview processor. A job-local compatibility config translates Tencent's earlier Python package namespace to the pinned Hunyuan3D 2.1 package name; the signed official safetensors checkpoint remains unchanged.

The installed provider manifest now includes the pinned Hunyuan3D-2mv config and weights. Deep provider verification passed, and a real offline runtime check loaded the official 4.93 GB safetensors checkpoint, `MVImageProcessorV2`, and a deliberately out-of-order front/left input into the expected labelled conditioning tensors. Automated intake, tamper, duplicate-role, approval-binding, persistence and provider-dispatch checks pass. The isolated v58 package passed its operational packaged smoke test at `release\prompt3d-workflow-20260904-58\win-unpacked\Grudge Dev Tool.exe`; packaged `app.asar` SHA-256 is `196FBA1E80CEA71B8E54825488A786671714248366E9DCFE7C8FB8CA2246707C`. It is running on the retained isolated acceptance profile for manual testing; the installed retail application remains running and untouched. A generated asset has not been semantically or visually accepted from supplied user images in this implementation run; that remains an explicit manual concept and multiview model review rather than an inferred success.

Closeout strengthens that path with a typed provider capability: Hunyuan declares one-to-four images, required front, supported front/left/back/right roles, exact 2.1-versus-2mv selection and fail-closed unsupported behavior. Intake and provider dispatch now reject identical bytes assigned to multiple roles as well as unsupported counts, roles, routes or undeclared models. The creation editor renders four real local thumbnails with each role, dimensions and hash plus reorder/remove actions; package-level DOM and visual inspection confirmed all four roles and the automatic 2mv/fail-closed notice without starting a provider request.

A fresh post-implementation offline RTX 3090 pass verified four distinct retained files and official Hunyuan background isolation, produced conditioning tensors `[1,4,3,512,512]` / masks `[1,4,1,512,512]` with indices `[0,1,2,3]`, and completed the declared 50-step, octree-512 generation. The 7,272,772-byte GLB has SHA-256 `2E71102D085260E9391719DCC69323DE2222BC757773146C8F50412B5BD91168`, 202,000 vertices and 403,996 faces. Six-view inspection confirmed real complete geometry but rejected the elongated, lumpy test form for semantic quality; `visualAcceptance` remains false, and texture/animation were not attempted. Exact record: `E:\GrudgePrompt3D\validation\multiview-runtime-postpatch-20260904\run-record.json`; inspection sheet: `multiview-inspection.png`.

Source typechecks, the focused reference/provider/approval/workflow/artifact/recovery/preload/sidecar suites, Python syntax, production build and isolated packaged operational smoke all pass. Final directory package: `release\prompt3d-reference-multiview-20260904-67\win-unpacked\Grudge Dev Tool.exe`; executable length `223309824`, SHA-256 `762DB7501667073C41361D3A4F8F004F4AF07EDB775536612FD3058A27339DEF`; `app.asar` length `77220248`, SHA-256 `71A3871AF905F7A336BBC82D5F217170D1A61E340FF931EDC4A769F466967A66`; packaged provider worker SHA-256 `A0899FD41B6ACB6824B8BDD8DDCD49451AF089575400D36C1409106ED56B148B`. Its smoke/UI-proof process trees and disposable profiles were removed, with no v67 helper or sidecar left. Retail and the pre-existing v62/v63/v64/v66 processes were not terminated or altered. No retail replacement, model download, deployment, commit or push occurred.

## Current: approved animation reaches final save — 2026-09-04

Live inspection of the isolated v55 package reproduced the reported stall: the current prompted animation was visibly approved, but the creation panel remained at Stage 6, **Refine or append animation**. The renderer incorrectly applied the strict acceptance-batch requirement for two approved animation prompts to the normal individual workflow, even though the backend save gate already accepts one exact approved prompted animation with retained Hunyuan geometry and Paint lineage.

The normal workflow now advances its first approved animation directly to **Save, export and reopen**, where automatic Local Files persistence is paired with an explicit **Save final model to Local Files** action. **Add or refine animation** remains available from the final panel, and optional editing includes **Keep approved animation and continue to save**, so entering refinement cannot trap or discard the accepted result. Strict serial-batch enforcement remains unchanged at two prompted and approved animations per asset.

The first v56 package proved the renderer transition but exposed the remaining main-process two-animation save gate through the retained runtime error log. After separating manual completeness from strict-batch baseline admission, v57 restored approved animation job `99e0d789-48a5-4c0b-b961-e0c5713e7dce` directly at Stage 8 and automatically saved it to `E:\GrudgePrompt3D\saved-assets\models\one-natural-quadruped-rabbit-in-a-low-forward-hopping--3777b19b-99e0d789.glb`. Independent managed-artifact reopening verified SHA-256 `8e175fae59f5c09a4adc10d3c82645b6c9bd3ddad0f4656a1133f782bf87437c`, unchanged geometry, two embedded texture sets, one four-second prompted animation, the sealed evidence bundle and final visual approval. The responsive package is running from `release/prompt3d-workflow-20260904-57/win-unpacked/Grudge Dev Tool.exe`; its `app.asar` SHA-256 is `7E41942F01088994A4F437E147911E8A0E3A259166EAD145DF37000903AA5B6F`. The installed retail app was untouched.

## Current: explicit finish rejection and replacement — 2026-09-04

Live inspection of the isolated v53 package confirmed that the animation review stage had no operational reject action and was still displaying the retained pre-v53 clip `Prompt Motion 56b3f584`. No animation had actually been generated after the direction-aware compiler change, so the apparently unchanged result was old playback rather than evidence from a new compiler run.

Texture and animation review now expose explicit classified rejection. The main process binds each rejection to the exact job, asset and geometry hashes, prompt, specification, stage-specific finish fingerprint and animation-plan hash, then retains it as immutable `visual-rejection.json`. The failed preview is hidden; a corrected sibling is allowed only from the exact approved parent and carries the rejected job ID in provenance. Animation correction is forced to replace the rejected clip set, and the clip-mode control is locked during recovery, preventing an old bad clip from remaining the default. Unapproved output is not implicitly treated as rejected, and rejected output cannot subsequently be approved.

The first replacement job `353f72b0-f420-460c-b7c4-3131a6c80b37` was not falsely approved: multi-view inspection showed a technically valid 0.6 m +Z path moving perpendicular to the retained right-facing rabbit anatomy, so it was explicitly rejected as `placement-path-mismatch`. The generic creature compiler now distinguishes subject-relative from absolute directions and maps forward/backward to the retained viewer-right pose without naming rabbit or any other requested example. In v55, corrected sibling `99e0d789-48a5-4c0b-b961-e0c5713e7dce` was generated from the exact approved texture parent with `mode=replace`, one four-second clip, a 0.6 m +X path and `grounded-stride` deformation. Time-separated front-view frames visibly moved nose-first to the right with alternating front/rear body regions; front, right, back, top and left views were inspected. The result remains unapproved for manual judgment.

Passed: both TypeScript projects, production renderer/main/preload build, preload bridge, authoritative workflow/rejection identity, renderer recovery/stage behavior, prompted-animation authoring including generic creature-facing regressions, strict batch approval/acceptance, artifact issuance, visual-inspection gates, fresh Windows directory packaging and packaged startup smoke. The responsive manual-test package is running from `release/prompt3d-workflow-20260904-55/win-unpacked/Grudge Dev Tool.exe` with profile `E:\GrudgePrompt3D\acceptance-profile-2026-09-03-v16`; the installed retail app was not stopped or replaced.

## Current: direction-aware prompted animation — 2026-09-04

Prompted animation now treats action and placement as separate instructions. Walk, run, swim, slither and fly create visible in-place body motion unless the prompt affirmatively supplies a direction, named path or measured displacement; only then is world-space root travel authored. Undirected hops rise and return in place. Negated direction, lift, spin, flap and trail terms are constraints rather than accidental requests. Default travel, orbit and hop scale from the current generated asset dimensions, and the creation panel explains the detected placement before submission while hiding the distance control for in-place motion.

The retained Rabbit evidence exposed the defect without altering history: three completed but unapproved attempts each contained only a 3 m root-translation channel and zero morph targets, and the latest `no significant vertical elevation` prompt was incorrectly compiled as a hop arc. The corrected generic compiler resolves that same prompt to a 0.6 m linear path plus alternating grounded-stride deformation, with the low-elevation constraint active. Structural inspection of a temporary child output found two animation morph targets, 113,647 affected vertex instances, maximum deformation about 0.04693 m and measured travel about 0.6 m. This is technical evidence, not visual acceptance; the old attempts remain unapproved and no retained workflow result was replaced or approved.

Passed: prompted-animation interpretation and GLB authoring, Prompt-to-3D workflow lineage/save, renderer recovery, strict-batch approval gates, motion-trail rendering, visual-inspection gates, both TypeScript projects, production renderer/main/preload build, fresh Windows directory packaging and packaged startup smoke. The responsive manual-test package is running from `release/prompt3d-workflow-20260904-53/win-unpacked/Grudge Dev Tool.exe` with profile `E:\GrudgePrompt3D\acceptance-profile-2026-09-03-v16`; `resources/app.asar` SHA-256 is `6C02F0364B9A0ED77E8A479F12AF68D13B4F21B4FBD4682F793E9FDCC5F1E0CB`. The previous isolated v52 process was closed. The installed retail app, approved texture and retained Hunyuan workflow files were not changed.

## Current: approved texture continuation to animation — 2026-09-04

An approved Hunyuan Paint result now exposes **Use approved texture and configure animation** in the single creation panel. The action retains the exact existing GLB/provenance/visual approval, persists the stage choice across a renderer restart, and enters the animation prompt/settings stage without launching another paint job. A return action keeps optional texture refinement available. Manual workflow save/export accepts one approved Paint revision after two approved animation revisions; the strict six-asset batch remains unchanged at two Paint prompts and two animation prompts per asset.

Passed: Prompt-to-3D workflow lineage/save, renderer stage/recovery, strict-batch approval gates, both TypeScript projects, production renderer/main/preload build, isolated Windows directory packaging, and packaged startup smoke. The fresh responsive test package is running from `release/prompt3d-workflow-20260904-52/win-unpacked/Grudge Dev Tool.exe` with profile `E:\GrudgePrompt3D\acceptance-profile-2026-09-03-v16`; `resources/app.asar` SHA-256 is `A5CE3C4DB162BE6A2BB5315CE955DF3D472E26689F4493C2CDD1F60444411D67`. The earlier isolated v51 process was closed; the installed retail app and retained Hunyuan workflow files were untouched. No new generation or animation was submitted in this repair.

## Current: unified 3D viewport mouse navigation and handoff — 2026-09-03

The inline asset/Prompt-to-3D preview and the full SceneEngine editor now share one mouse contract: left drag pans horizontally/vertically, right drag orbits the camera angle, middle drag dollies the camera position, and the wheel continues to zoom. The old Shift-key handler that could restore the previous left-drag orbit mapping was removed. Each asset or workflow-stage handoff reapplies the shared contract, clears stale interaction state, and displays the current navigation context plus the last completed action in the viewport.

Passed: focused mapping/handoff test, both TypeScript projects, Prompt-to-3D visual-inspection and renderer-recovery regressions, production renderer/main/preload build, and isolated Windows packaging. In the visible v50 packaged app, the retained approved Rabbit reopened at **Refine the texture prompt and seed · Stage 4 of 8**; left drag moved it in view without changing its angle, the status returned from active to **Preview navigation ready · Last: Move X/Y**, and wheel zoom remained active with **Last: Wheel zoom**. Right/middle assignments are confirmed by the shared compiled mapping and focused test; the available screen-control driver itself exposes only left-button drag.

The isolated package is running from `release/prompt3d-workflow-20260903-50/win-unpacked/Grudge Dev Tool.exe`; executable SHA-256 is `AAAE6848572DC52DDF9BFBA6F18628709B7BAE522EF9DAF46DD0D31B8CDF8F8A` (223,309,824 bytes). The approved Rabbit and its Stage 4 workflow state were preserved. No texture prompt was entered or submitted, no generation ran, and no package was promoted or published.

## Current: repeated Hunyuan concept-edit lineage repair — 2026-09-03

Prompt-edited Hunyuan concept successors now advance the retained seed exactly once, and final shape-refinement validation accepts multiple genuine prompt edits instead of requiring every successor after the first to be seed-only regeneration. The validator still requires the first refinement edge to be an explicit edit, exact successor prompt/plan/spec/inspection hashes, classified rejections, and continuous provider/reference bindings. A narrow read-only compatibility path accepts same-seed prompt edits written by earlier builds only when all retained bindings remain exact; new same-seed edits are rejected at service intake.

Passed: Prompt-to-3D workflow lineage/save, rejected-refinement/fresh-root lineage, renderer recovery/stage-panel behavior, concept approval/provenance, both TypeScript projects, production build, sandboxed preload, and packaged runtime smoke. The compiled validator accepts the exact retained rabbit chain from base job `3777b19b-676a-4d40-9175-104eee12b3e7` to refined job `e29772d8-e29c-46b6-a710-3463ddc8ba4a`, resolving first refinement `41f0887a-cd70-4c60-932e-acc8c93b91c8` and immediate concept parent `b7b36aad-4fac-422c-8e81-bf93acd8a4b5`. No retained job or provenance file was rewritten.

The isolated v46 package is running from `release/prompt3d-workflow-20260903-46/win-unpacked/Grudge Dev Tool.exe` with profile `E:\GrudgePrompt3D\acceptance-profile-2026-09-03-v16`. Its `app.asar` SHA-256 is `8d8bb14cd668f1a3e667ff8616a9cb6ee32612547608f7e3b9a16a430801b825`. Visible restart inspection restored the rabbit geometry at **Texture prompt and seed** with no workflow error displayed. The separately installed application remained running and untouched. Texture generation was not started; manual acceptance testing resumes from this point.

## Current: cosmetic detail and bounded structural refinement — 2026-09-01

The original procedural workflow now accepts ordinary follow-ups that either add authored cosmetic detail meshes or make significant, bounded changes to named parts of the current sword, game prop or segmented person. Each request saves a new child revision with a new geometry hash while preserving the asset ID, embedded textures, animation clips, prior revision and provenance. The UI shows the supported refinement prompts, detail-node count, applied changes and previous/current geometry identity. Unsupported arbitrary sculpting is not claimed.

**Passed in one real packaged session:** each of the three previously completed assets was reopened from retained history, enhanced with `Add detailed cosmetic additions to it.`, then structurally adjusted through the normal Grudge prompt UI. The sword gained a fuller/inlay, ricasso collar, guard caps and pommel inlays before blade length +25%, blade width +30%, increased curvature and guard width +35%. The game prop gained 14 vent/coil/sight/stud nodes before length +28%, bulk +24% and muzzle size +30%. The person gained ten shoulder/cuff/belt/knee/boot nodes before height +16%, breadth +18% and head size +22%. Textures and the existing sword swipe, muzzle projectile and 12-node articulated dance remained operational. Time-separated viewport frames visibly confirmed each motion after alteration.

The passing build is `original-flow-2026-09-01.2`, packaged at `release/original-flow-cosmetic-trial-02/win-unpacked/Grudge Dev Tool.exe`, using isolated profile `E:\GrudgePrompt3D\portal-verification-profile-cosmetic-02`. A clean quit/relaunch of only this trial restored the remembered local-controls choice and all 15 revisions. The final sword, game prop and person were reopened from local storage and their embedded textures/clips replayed without regeneration. The existing user app at `release/win-unpacked` remained running and untouched.

Focused CPU checks passed all 15 enhancement/adjustment assertions after retaining the first harness-typo failure. Both TypeScript projects, preload check and production build passed. The final immutable revision IDs are `f4b10f96` (sword), `471f1224` (game prop) and `7cca4a84` (person). Evidence and the illustrated replay guide are under `C:\Users\mjneu\Documents\Codex\2026-08-31\run-x20\outputs\grudge-cosmetic-adjustment-trial-02`.

Scope remains basic stylized CPU-authored assets and bounded deterministic part changes. The character is segmented rather than a skinned production rig, the game prop/projectile are cosmetic, optional Ollama planning and neural providers were not used, and Hunyuan commercial licensing remains externally unresolved. This isolated unpacked package was not promoted, published or installed.

## Current: continuous original creation flow — 2026-08-31

The latest design uses the existing Category and Style dropdowns and an optional local planner. It supersedes the older separate Object/Component panel and mandatory concept-choice proposal below. Original procedural authoring is an explicit creation method, separate from the installed neural generators. It creates basic original sword/game-prop/person geometry, applies authored pixel textures, and adds handle-pivot swiping, a cosmetic muzzle projectile or joint-articulated dancing without loading borrowed preview bodies.

**Passed in one real packaged session:** curved sword → texture → swipe; original game gun prop → texture → visible muzzle projectile; original segmented person → texture → articulated dance. All nine commands were entered through normal screen-controlled portal UI. No helper-generated asset, direct API injection, inference model or repair/restart between chains was used. All three final saved GLBs were then reopened through visible local-history controls, and their textures and motion played without regeneration. Each chain retained one base geometry identity and separate immutable stage files. The game projectile adds a separate effect mesh without replacing the prop.

The passing build is `original-flow-2026-08-31.1`, packaged at `release/original-flow-trial-01/win-unpacked/Grudge Dev Tool.exe`, running with isolated profile `E:\GrudgePrompt3D\portal-verification-profile`. App archive SHA-256: `d8a5ad994905c29a82c56e7f6e1c90dec3e86c231e9e093775da7cf93a0ce49f`; all 484 generated build files match that archive. Both TypeScript projects, renderer/main/preload build and focused CPU GLB checks passed. The existing user app at `release/win-unpacked` was not replaced or stopped for this trial.

An additional clean quit/relaunch of only the isolated trial passed fresh-process persistence at 23:16 Perth: nine stages and preferences restored, and all three final local GLBs visibly replayed through normal portal controls. No regeneration or rebuild occurred; all nine asset hashes and attempt timestamps remained unchanged. Separate screenshots 32–42 and `post-restart-persistence-evidence.json` supplement the original continuous-run evidence without replacing it.

Guide and 31 actual portal screenshots: `C:\Users\mjneu\Documents\Codex\2026-08-31\realtime-voice-chat\outputs\grudge-flow-trial-01\Grudge-original-creation-step-by-step.md`. Adjacent CSV/JSON evidence retains exact prompts, settings, timestamps, asset/attempt IDs, hashes, clips, embedded images and replay records. Assets live in `E:\GrudgePrompt3D\creation-history`. Previous incomplete trial, development check artifacts, earlier swords/axe, libraries and the audit remain retained; no history cleanup was performed.

Scope: basic stylized CPU-authored assets, procedural texture maps and rigid segmented joint articulation, not a skinned production character or broad neural quality proof. Optional Ollama planning was off. No new provider/model download, neural paint, game-engine import, installer publication or Hunyuan commercial clearance is claimed. Hunyuan/DiT licensing remains escalated externally. Historical sections below describe earlier builds and are not the current trial package.

## Object/component rules — 2026-08-31

Implemented typed profiles, component-specific shape/orientation rules, a visible compiled generation prompt, retained surface brief, attachment-origin export and explicit bounds/flip controls. Both local workers consume the main-compiled prompt; approved concepts are invalidated when generation rules change. Structured concept-review errors no longer expose a progress traceback for expected white-background/framing rejection. Character/vehicle and provider security gates remain intact.

TypeScript and renderer/main/preload builds passed. A CPU check compiled the user's actual failed axe brief to a single double-headed axe with grip down/head up and a white background, excluding its cinematic/dark-background prose. Actual GLB postprocessing/validation passed base, centred, top and custom flipped origin cases, preserving the input file. These are numeric-placement checks using an existing mesh, not semantic proof of an axe or socket. No new GPU generation was started.

The exact packaged runtime passed object/component prompt changes, attachment defaults/custom values, original-brief preservation and persistence across navigation, with zero uncaught renderer exceptions. Local Files fallback/preview/recovery/refresh also passed. All 474 build files and Python helpers matched. Promoted and launched `release/win-unpacked`; app.asar SHA-256 `a5b0d278baf5ebf07f1f54c122989b7dda19c79b7bee47dc53fd7259ccf0d0a6`. Rollback: `release/win-unpacked-before-object-rules-20260831`. The user-started job `4662c405` was complete before replacement, and profile preferences were byte-for-byte unchanged. Existing results remain in place.

## Sword quality and chat refinement — 2026-08-31

The investigation confirmed cropped concepts, opaque backgrounds, non-uniform scaling and a structural-only quality label. The revised integration preserves proportions, keeps raw meshes, seeds the shape stage at 512 resolution, constrains simplification and offers concept review/reuse before geometry. Technical success no longer claims artistic or semantic acceptance.

Actual local Hunyuan trials: `8e43da33` produced a complete recognizable longsword (49,999 triangles), inspected from four sides. `ec879f79` and `575d08d2` were technically valid but missed the requested sabre/leafblade profiles and were rejected as examples of those types. Concept-only trial `239d0290` completed without geometry; its curvature remained weak. Installed source/weights passed the existing signed/deep integrity verification, with no new model download or cloud generation.

The existing local Forge now has an explicitly separate precision sword path for reliable named blade/guard/grip/pommel geometry. Real local chat created longsword, sabre and leafblade recipes; all were accepted/saved through the UI. The first renders led to a taper correction; the final versions were regenerated and inspected from multiple sides. These are procedural assets, not proof of neural-model fidelity.

Local Forge chat also previews procedural texture regions and rigid-object turntable/swing/thrust clips, supports before/after, discard, scrubbing, duration/pivot and blade adjustments, and saves each acceptance as a new GLB/recipe. The existing `grudge-dev:latest` weights at `E:\AI\ollama\models` were connected through native loopback CPU Ollama on port 11435. The app can deliberately start that installed service; it never pulls weights. Inference uses CPU and unloads after each request. Existing shared Docker Ollama settings are unchanged.

Passed: TypeScript checks, renderer/main build, bundled preload check, real local chat → texture/animation preview → independent GLB save in the packaged runtime (one embedded image, one animation, zero uncaught renderer exceptions), and all three precision creation/save paths. The earlier Local Files fix also passed again in the final package: five initial image failures, no retries/broken images, successful preview recovery and folder refresh.

User-facing artifacts: `C:\Users\mjneu\Documents\Codex\2026-08-31\run-x20\outputs\sword-review\index.html` and adjacent GLBs/renders/recipes. Neural paint, skeletal animation, automatic semantic part recognition, production UV unwrapping, game-engine integration and watertightness are not claimed. GPU generation is finished; no user-owned process was stopped.

Promoted the verified unpacked build to `release/win-unpacked/Grudge Dev Tool.exe` and observed its responsive window after launch. All 472 build files and packaged Python helpers matched the source build. Final `resources/app.asar` SHA-256: `f4afe6de821989032a51bd9ac0b6f94900b813cd0a4a11bfd6d42d8918a38cda`. Rollback is retained at `release/win-unpacked-before-sword-refinement-20260831`. A further isolated check of this exact package passed Local Files preview/fallback/refresh with zero uncaught renderer exceptions.

The user's original brief and enabled local-controls choice were compared and preserved during replacement. The dedicated CPU planner endpoint is saved for the real profile; no model remains loaded between requests. The temporary Vite preview helper was closed. The older registered installed app initially opened by Windows was closed, and the verified portable build was launched explicitly. No published installer, signing assurance, remote deployment, or game-engine acceptance is claimed.

## Purpose and current work

Electron desktop asset browser, media/3D viewer and local Prompt-to-3D tooling. Existing architecture and operating instructions remain in `AGENTS.md`; feature history is in `CHANGELOG.md`.

The Local Files repair and Prompt-to-3D persistence update are built and running. An isolated run of the old Windows package reproduced 46,258 image failures from four file rows after remote icon requests were blocked. The error handler reloaded the same unavailable remote fallback indefinitely.

The source repair uses a fixed-size local icon while the remote icon loads, removes a failed remote image instead of retrying, and applies the same stable fallback to file rows, the header and sidebar. Remote icon URLs and security settings are unchanged.

Local controls are now a single remembered checkbox. The observed enabled choice was carried across the package replacement. The asset brief is saved through main-process storage, with a renderer mirror for quick display. Job snapshots and previous results are retained; legacy results recover from existing files. A new or failed generation does not replace the last successful preview. Interrupted work never resumes automatically.

## Evidence and boundaries

- Confirmed: old packaged runtime reproduces the retry loop, using an isolated offline profile and software rendering. The fixed package stops at five initial failures, displays local fallback icons, and makes no repeated image requests in the observation interval.
- Passed: build, both TypeScript projects, sandboxed preload check, valid/invalid image preview recovery, and folder refresh. Packaged checks completed without uncaught renderer exceptions.
- Passed: three separate packaged-process runs confirmed enabled and disabled choices survive restarts, drafts survive immediate process exits, and disabling revokes privileged controls. Fresh profiles default to disabled. No models or generation were started by these checks.
- Passed: job snapshots retain previous results, older job files are recovered, interrupted jobs are not resumed, paths outside the selected root are rejected, and a corrupt snapshot does not hide other valid results.
- Running: `release/win-unpacked/Grudge Dev Tool.exe`; responsive window observed after replacement. Prompt-to-3D visibly reports `Autosave on`, `Saved`, and `Retain previous result on`, and restores the completed sword result without regeneration.
- The user's active sword generation was allowed to finish at Complete / 100% with validated output before the old app and its idle sidecar were closed. Its output files were not moved or changed.
- Rollback: `release/win-unpacked-before-ui-state-fix-20260831`. All 466 packaged build files matched the current build before promotion. New `resources/app.asar` SHA-256: `3fac0dd1de064934347c5b2e450c7de2401078709f0f6f380f3bc8b53df7f323`.
- Existing unrelated local edits are preserved. No production release, publication, credential change, model download or generation is part of this repair.
- GPU workload ownership remains protected; no user-owned workload is stopped for this task.

Remaining limitation: this is a local unpacked package, not a published installer/release. The existing auto-update warning about absent `app-update.yml` remains outside this repair. No new GPU generation was needed to validate persistence; actual saved-result recovery was observed in the running app.

# Prompt-to-build end-to-end campaign

Run the current Dev Tool from its saved main checkout. Prefer installed grudge-dev for prompt interpretation and exercise existing geometry, texture, motion, reuse, editing, scene persistence and provider paths. Preserve earlier user assets and running packages.

1. Repair current-main build regressions without dropping the newly merged editor/play functionality. Restore the declared GrudgeStudioNPM dependency and verify both TypeScript projects, renderer, main and preload.
2. Execute real local model requests and complete create, adjust, texture, animate, save and reopen chains for representative supported shapes, sword, cosmetic game prop and segmented person. Record exact prompts, planner models, artifacts and failures.
3. Fix prompt routing and execution gaps against the existing creation services and Forge editor. A successful plan or navigation is not an executed build. Unsupported requests must report the missing capability instead of inventing success. World construction must be distinguished from mesh repair.
4. Verify existing Hunyuan/TRELLIS/HY-Motion installations and exercise the available enhancement path with exact provenance and real visual inspection. Do not bypass concept, geometry, texture or motion review or substitute a different provider.
5. Package the repaired application, run isolated packaged checks, inspect real models and motion, and verify save/reopen and editor handoffs. Preserve local assets and keep the report explicit about visual or provider gates that remain unproven.

Evidence goes in `.cache/prompt-e2e-20260909` and a user-readable campaign report, with current status in PROJECT_STATUS.md and CHANGELOG.md. No release publication or installation over the existing user package is part of this request.

The user requested execution in this task; the toolkit planning-only session boundary does not apply. Routine implementation choices are resolved from source and current instructions.

Continuation scope: extend the one-prompt entry point across existing local app controls, add immutable component operations, repair every exposed execution failure and rerun native/package evidence. Keep control-planning fixtures, actual saved GLBs, visible results and unsupported embedded/native surfaces distinct. No universal completion claim without execution evidence for those surfaces.

Current continuation: recover native control, close the blocked handoff/component/field checks, add exact local path actions, fix compound field completion and preserve significant input/status text. Verify the final package with normal UI prompts, missing-path failure and the actual saved GLB. Refresh provider readiness without installing or disturbing other GPU jobs.

Embedded continuation: ignore Hunyuan/hardware restrictions and focus on the five existing embedded apps. Add owned/origin-bound observation and fixed control execution; verify real Electron transport, installed Grudge planning, packaged preload and native live results. Correct discovered display, stale-page, text, navigation and placement gaps immediately. Keep fixture results separate from live acceptance.

Embedded execution update: the campaign passes live ThreeFlow navigation/search, primitive/repeat placement and palette recovery, Builder +Box, and the previously failed hidden-Properties rename/position prompt (v44). Final v46 build, 22 transport cases, package smoke and packaged-file identity pass; corrected click-once wording has a separate real-Grudge regression. A live save/reload retained data but lost lighting, so the upstream source was repaired locally. Its build, browser restore/hierarchy check and three-round-trip helper/particle/transform regression pass. The unapplied upstream patch is reviewable in docs/patches/threeflow-embedded-20260909.md. Remaining production deployment, signed-in access, specialised editors/native gestures and full world-system acceptance remain documented; universal completion is not established.

# Decisions — 2026-09-05

- Implement the explicit current full-work request through review and packaging in this saved main checkout. Phase-only toolkit stops, separate naming approvals and unsolicited branch creation do not apply.
- Preserve the captured dirty Dev Tool baseline and review only task-owned changes; no commit, push, deployment or provider mutation.
- Consolidate the three concept pass controls into one optional comprehensive explicit attestation while retaining the three persisted checks, detailed controls and all later visual gates.
- Add raw-text numeric editing after fresh review proved browser number inputs sanitize incomplete values. Exercise actual keyboard entry in the isolated package.
- Retain updater state in main with a snapshot IPC after review found that renderer reload lost the restart action. Reset install state on event-reported errors.
- End negation only at clear contrasting words; temporal `not yet` must preserve negation. Add actual compiler regressions for both forms.
- Extend the runtime work to locally actionable GrudgeBlox guide items GB-C1/E1/E2/E3/E5 under the user's request to finish discussed work. Keep backend ownership GB-E4 explicit and unresolved.
- For GrudgeBlox transient effects, choose the guide's private geometry/material clone ownership alternative; shared textures and cached prototypes remain owned by the cache.
- Retire client session entities/events and reject old async completions when adding unmount cleanup. Use a separately named renderer cleanup because Three.js installs its disposer on the instance.
- Keep Next 15.x and refresh compatible dependencies. Use two narrow reviewed/tested overrides for Next's pinned PostCSS and node-three-gltf's Sharp rather than a wholesale framework/adapter major upgrade.
- Package the final reviewed Dev Tool as an NSIS installer. Applying it to the active owner session remains a separate close/restart decision; the installable artifact is already built and verified.

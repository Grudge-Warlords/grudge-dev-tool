# Guided workflow reliability implementation

Extend the current workflow and validation seams in the saved checkout. No branch, dependency upgrade, provider download or live deployment.

1. Add shared configuration/optional-number validation; use it in NeuralPrompt3D and service admission. Hide optional settings in accessible details controls, retaining summaries and contextual errors. Add missing custom-style input.
2. Extend the existing prompt-motion interpreter with bounded unsupported-transformation checks and distinguish return-to-place wording from unsolicited travel. Enforce before authoring and show the same explanation beside the motion prompt.
3. Correct both existing fleet probe implementations with abort deadlines, response cleanup and API-content checks. Make doctor success criteria identical in text/JSON.
4. Repair save-progress/error feedback and stale readiness responses. Add explicit updater state transitions and duplicate-action guards while preserving opt-in downloads and install.
5. Run regressions for actual invalid inputs and network failure behavior; build, package in a new output directory and exercise its renderer with a disposable offline profile. Review only the delta from the captured dirty baseline.
6. Update PROJECT_STATUS.md, CHANGELOG.md and a chat-to-source audit with verified changes and unresolved acceptance/activation dependencies.

Acceptance: all eight criteria in the adjacent requirements are carried into these steps. Steps 1-4 cover behavior; step 5 supplies source/build/package/runtime evidence; step 6 records scope and limits. Generative semantic acceptance must never be inferred from these tests.

When a settled decision deviates from or extends this plan, append one line with its reason to guided-reliability.DECISIONS.md beside this plan.

# Prompt-to-build campaign — 9 September 2026

## Current continuation: embedded tools

The owner asked to set Hunyuan/hardware issues aside and focus on embedded actions. All live prompt checks below used installed `grudge-dev:latest`. No provider install, cloud fallback, publication, deployment or installed-app replacement was performed.

Builder, Forge, Coder, ThreeFlow and Preview now expose their existing labelled controls through a fixed isolated-world bridge. Main verifies guest ownership and supported origin, retains the exact observed document and controls, and consumes each action receipt once. Navigation or changed values require a fresh plan. Remote pages receive no desktop preload bridge. Session handoff checks the current guest origin and exact URL again before injection and returns the actual injection outcome.

Supported controls include buttons, text/number fields, selects, ARIA toggles, tabs, plain editable text and existing DOM drag/drop handlers. Significant whitespace is preserved or rejected before a lossy edit. ThreeFlow adapters associate its existing tab, catalog, property and axis labels with the corresponding controls; property edits retain the selected-object context. Builder's existing lab is available under More tools. Forge, Coder and Preview retain their normal profile/admin requirements.

| Native prompt / check | Actual result | Evidence under `.cache/prompt-e2e-20260909` |
| --- | --- | --- |
| Open ThreeFlow and set Search to "frostbite" | v35: 2 actions; live world catalog visibly filtered | `phase5-live-threeflow-search-v35.jpg`, `.txt` |
| Open Builder and click the + Box button once | v40: 2 actions; navigation and one actual box insertion; Finished | `phase5-live-builder-box-v40.jpg`, `.txt` |
| Open ThreeFlow and open Place, then drag Box to Scene viewport | v42: 3 actions; white box visible, editor selection confirms BoxGeometry; Finished | `phase5-live-threeflow-placement-v42.jpg`, `.txt` |
| In ThreeFlow, add a box to the scene | v42 repeat: Select Camera, one drag, changed selection verified; Finished | `phase5-live-threeflow-repeat-v42.jpg`, `.txt` |
| In ThreeFlow, add a sphere to the scene, starting with command palette open | v42: closes actual palette then one Sphere drag; SphereGeometry selected; Finished. Geometry overlaps the earlier box, so this is selection/dispatch evidence rather than a separate unobscured sphere view | `phase5-live-threeflow-palette-v42.jpg`, `.txt` |
| Open ThreeFlow and add a box to the scene | v44: 3 actions; fresh visible box; Finished | `phase5-live-threeflow-placement-v44.jpg` |
| In ThreeFlow, set Name to "Coverage Box" and Position X to 3 | v44: opens Properties from Material, changes only the named fields; Name and X=3.000 visible and box moved; Finished after 3 actions | `phase5-live-threeflow-properties-v44.jpg`, `.txt` |
| In ThreeFlow, click the Save button once | v44: one Save click; completion wording incorrectly invented a saved hierarchy result. v45 corrects that wording and its real-Grudge regression passes | `phase5-live-threeflow-save-wording-v44.jpg`, `.txt`; `phase5-once-regression-v45.log` |
| Manually reload the saved live ThreeFlow scene | v44: Coverage Box and X=3 retained, but lighting lost. This is a visual restore failure despite data persistence | `phase5-live-threeflow-reload-v44.jpg`, `.txt`; `phase5-live-threeflow-reloaded-transform-v44.jpg` |

Live failures were retained and corrected immediately:

- v34: terrain canvas covered the embedded editor. It now renders only in Terrain mode.
- v36/v37: palette coverage and scroll-clipped catalog items stopped placement. Existing tabs, modal closure and scroll reachability now have semantic adapters.
- v37: Builder's initial blank/loading page was treated as an unsupported origin. Loading checks and fresh terminal observation now let startup complete. A later duplicate-click attempt led to strict click-once planning; v40 succeeds.
- v40: the centre of the scene viewport was covered by a panel. Drag execution now uses an exposed point and still refuses a fully covered drop area.
- v41: the box was placed, but the upstream ThreeFlow hierarchy did not refresh, causing an unnecessary completion timeout. The examined source adds/renames the mesh without invalidating the tree. The Dev Tool now verifies either hierarchy growth or a changed public selected-object caption after an applied drag. Repeating the same primitive first selects Camera, so an unchanged old box cannot count as a new creation. The upstream repair below corrects the tree locally; production has not received that patch.
- v43: while Properties was closed, a compound field request pressed an unrelated number control and filled the hierarchy filter. v44 retains requested field names before their panel opens, opens Properties for its known fields, and blocks unrelated fields/buttons or premature completion. Regression and native replay both pass; `phase5-live-threeflow-wrong-field-v43.jpg`/`.txt` retain the failure.
- v44: an explicit Save click completed with an invented success narrative. v45 binds click-once completion wording to the observed activation; it no longer claims an unobserved saved revision. v46 also supports the repaired upstream selected-object marker and avoids adding a duplicate palette Close button.

Validation: **22 current Electron transport cases pass** across synthetic guest pages. Separate actual Grudge runs passed five compound-setting workflows (one per embedded surface), hierarchy-confirmed placement, click-once completion, two stale-hierarchy selection workflows, and the hidden-Properties compound edit. The final property run reports 23 cases: 22 transport cases plus that real-model workflow. Repeated base cases are not additive. Fixtures prove the app bridge and planner, not signed-in production acceptance.

Final v46 full typecheck, production renderer/main/preload/icon build, 22 transport checks and packaged operational smoke pass. App-action regressions are v44; the corrected real Grudge click-once run is v45 (23 cases: 22 transport plus one real-model workflow). Smoke includes rejection of invalid embedded targets and receipts. Logs retain their exact candidate suffix; model fixture results are `phase5-grudge-embedded-fixtures.json`, `phase5-once-embedded-fixtures.json`, `phase5-selection-embedded-fixtures.json` and `phase5-property-embedded-fixtures.json`. Native production acceptance remains the specific v35–v44 runs above, not an assertion that every case was replayed in v46. Earlier failures and candidates remain retained.

- Package: `release/prompt-e2e-20260909-v46/win-unpacked/Grudge Dev Tool.exe`.
- EXE SHA-256: `ca7e1bd110fad0f7d0beb92946473923a2b7cf6e0ab08b702ddba9935a6efe3a`.
- ASAR SHA-256: `3a61d062b6466ce4bd5fcf0b69519f3eb463d1fc65c1c29cffe330a7ca50ed91`.
- `phase5-v46-package.json` confirms packaged planner, bridge, shared validation, preload and isolated guest runtime match the built files.

### Upstream ThreeFlow scene repair

The live save/reload failure was actioned in a local clone of `MolochDaGod/ThreeFlow` at `cfe58bc9ab088b303c4412e3415116d523243c58`. The loader disposed the newly parsed scene, and Three intentionally omits generated render-target environment textures from JSON. The repair preserves loaded resources and regenerates the editor's room environment, while retaining explicit environment modes. It also refreshes hierarchy/selection after creation and renaming, repairs the missing palette/registry and baseline source build errors, removes saved editor helpers, retains authored particles, captures pending transforms and replaces measurement/mouse bindings correctly.

The local production build and a three-round-trip real-Three snapshot regression pass. Browser UI acceptance passes for primitive creation, immediate tree update, rename and X=3, save/reload with a lit box and textured ground, and repeated reload with no duplicate helper rows. The command palette opens, filters and dispatches its existing Save command. Screenshot and current values are retained in `phase5-threeflow-local-restored.png` and `.txt`. These are **local editor UI checks**, separate from the installed-Grudge prompts against the production site.

An additional integration run uses the actual locally built editor in a private Electron session with the fixed v46 bridge and installed `grudge-dev:latest`: add Box (2 actions), set Name to "Integration Box" and Position X to 3 (3 actions), and click Save once (1 action). All three prompts pass. Save reports its actual completion in the editor; a new document is then loaded and the retained name and X=3 are checked through the fresh bridge. The final screenshot shows the lit box, textured ground and restored field values. These are three separate prompts, not a claim of a combined create/edit/save prompt or production deployment. Evidence: `phase5-threeflow-integration/results.json`, `restored.png`, and `phase5-threeflow-integration.log`; runner: `scripts/test-threeflow-integration.mjs` with an explicitly supplied loopback build origin.

The first integration attempt's screenshot exposed a reload race in the test: it could inspect the old document before navigation started. That attempt is retained as `attempt1-reload-race.json`, `attempt1-loading.png` and `phase5-threeflow-integration-attempt1.log` and does not count as reload acceptance. The corrected run waits for navigation completion, a different isolated document identity, editor readiness and fresh paint before verification/capture. Catalog thumbnail availability and complete asset-library coverage are outside this integration assertion.

The [reviewable upstream patch and reproduction notes](patches/threeflow-embedded-20260909.md) are saved in the Dev Tool repository. Patch application against the exact upstream base passes. Local vendor preparation used existing Grudge package sources; no canonical vendor source changed. This patch has **not been pushed or deployed** and the live editor still needs that correction. Custom environment modes and every import/gameplay route have not received visual acceptance.

Universal completion remains unestablished. Live signed-in Forge/Coder/Preview workflows, specialised code editors, arbitrary canvas gestures, general native dialogs, connected writes and complete coding/gameplay/world implementations still require acceptance. Hunyuan/hardware work remains outside this continuation as requested.

## Earlier continuation: native completion checks and local paths


The Windows controller recovered in this continuation. The previously blocked native handoff, component edit/save/reopen and numeric editor-field tests now pass. These are normal packaged app interactions through Ask Grudge using `grudge-dev:latest`, followed by inspection of the actual results.

| Native prompt / action | Result | Evidence |
| --- | --- | --- |
| Open the current model in Forge and frame it | v29: 2 actions; retained green cube loaded and frame result visible | `phase4-forge-handoff-v29.jpg` |
| Open Prompt to 3D, add a red sphere named Beacon, one metre in diameter, at position [3,0.5,0] and save it | v29: 3 actions from Forge; two meshes, 92 triangles, original cube retained | `phase4-component-v29.jpg` |
| Reopen that saved revision | Both components visible again; cube still turns; exact saved bytes match | `phase4-component-reopened-v29.jpg`, `phase4-native-component-result.json` |
| Open the saved-model folder by its exact path | v30: 4 actions from creation; correct folder and two saved GLBs visible; no native picker | `phase4-open-folder-v30.jpg` |
| Open the saved model by its exact path in Forge | v30: 2 actions; exact retained two-part model visible | `phase4-open-model-v30.jpg` |
| Set Exposure to 1.2 | v30: 1 action; actual editor shows 1.20 | `phase4-exposure-v30.jpg` |
| Set Exposure to 1.35 and Key light intensity to 0.8 | v32: 2 actions; both actual values match, no early completion | `phase4-multi-settings-v32.jpg`, `phase4-multi-settings-v32.txt` |
| Open a missing model from inside Forge | v33: 3 actions; exact missing-file result and existing scene retained | `phase4-missing-path-preserved-v33.jpg`, `.txt` |
| Open a valid model after that failure | v33: 2 actions without restart; prior model retained, 2 scene objects and 104 triangles | `phase4-path-recovery-v33.jpg`, `.txt` |

Native component revision: `b704829f-e9d0-4d05-a80a-c1bb990c3b37`; SHA-256 `d680f6267e48893903cf9d143d8fe1d44ee5d279ef493090056b758388438348`. Inspection of the actual native output proves Beacon is red at [3,0.5,0], the original cube material and texture bytes are unchanged, and all original animation channels and key values are unchanged. The source hash remains `08a2978deda0158336c793604f4ed130e6695901c166eaf3b4a3dc41281334bf`.

New repairs and coverage:

- Local Files has a collapsed **Open a local path** control for folders and explicit Forge model handoff. Full local drive paths are validated and bound exactly; missing files must report failure. Folder/model handlers reuse the existing app services. No general native-dialog automation is claimed.
- Literal setting requests now bind every completely understood assignment and cannot finish while any requested field has the wrong value. Unknown clauses are not silently discarded. Numeric formatting, quoted multiline text, significant spaces and option values are preserved.
- Narrowed planner schemas now send only candidate controls while retaining screen result evidence and history. Grudge remains the actual local planner. No measured speed multiplier is claimed.
- Creation completion messages describe a saved revision. The editor reports successful and failed model loads, and its header no longer consumes an unintended grid row.
- A live result exposed boolean React state markers being read as the word `true`; the final build reads their actual visible state again while retaining explicit path status exactly. Regression checks cover both representations.

Final source typechecking, app-action regressions, production renderer/main/preload/icon build and packaged operational smoke pass for **v33**. Logs use `phase4-*-v33.log`. Earlier v30/v31/v32 packages and evidence are retained. v32 compound settings passed. Its missing-model test correctly failed, but exposed a vague error and unnecessary navigation that emptied the current editor. v33 keeps model-path opening inside Forge, preserves the current scene on read/parse failure, and carries the actual failure into the prompt result. The native v33 failure rerun passes: exactly three actions, a specific File not found result, and the existing scene/model still visible. Evidence: `phase4-missing-path-preserved-v33.jpg` and `.txt`. Valid-path recovery also passes in the same v33 session: two actions open the original green cube alongside the existing component scene, with two scene objects and 104 total triangles. Evidence: `phase4-path-recovery-v33.jpg` and `.txt`.

- Package: `release/prompt-e2e-20260909-v33/win-unpacked/Grudge Dev Tool.exe`.
- EXE SHA-256: `DA8BD053FBD9D3B081C943BB67A66A25C8643F14CE73DAA1F2BF49B1266C0401`.
- ASAR SHA-256: `2AC02B59C97BEEFA1FCF5BD58821A994DF50A0B21CEB5013C7044417B4331572`.
- Fresh provider overview: `phase4-provider-overview.json`. Hunyuan shape is installed and ready; HY-Motion and TRELLIS still require setup. The current adapter has about 19.9 GiB free, below the Paint profile's 21 GiB requirement. No new successful Paint chain is claimed.
- Universal completion is **not established**. Embedded application contents, general native pickers, canvas gestures, full coding/gameplay/world systems, skin duplication and all connected/admin actions remain outside proven one-prompt coverage. These are explicit remaining gaps, not successful actions. Existing user apps, assets, dirty main, installed app and public services are preserved.

## Historical phase 3: app-wide prompts and component editing

The new **Ask Grudge** control is mounted above all 25 signed-in route mounts, including hidden aliases and restricted routes. It observes the current app controls, asks the installed local Grudge model for one bounded decision, activates the existing control, checks the result and continues. It does not certify all 25 routes: the direct-source inventory is retained in `.cache/prompt-e2e-20260909/phase3-route-inventory.json`, and live coverage is listed separately below. It operates labelled buttons, text/select fields, toggles and disclosures. Canvas gestures, native file dialogs and embedded webview contents still need dedicated adapters. The five directly embedded route sources are Builder, Forge, Coder, ThreeFlow and Preview; their surrounding local controls are distinct from the embedded application's controls.

Creation prompts use the existing compound creation runner once, with the full instruction retained. Grudge remains the preferred installed planner, running on CPU with explicit context bounds. There is no automatic model download or cloud fallback. Explicit Hunyuan/TRELLIS/HY-Motion requests route toward their existing provider workflow; negated provider requests stay on the built-in route. Existing provider readiness and visual review gates remain in force.

Component edits now include **add, duplicate, rename and remove**, alongside earlier transforms/colors/motion. Exact names, dimensions and positions are bound; duplicate names and missing later targets are rejected before any partial edit in an edit/save-only plan. Copies preserve shared geometry without recoloring their originals. Animated translation copies retain their placement offset, including cubic-spline values without changing tangents. Skinned-part copies still reject and direct the user to Skeleton Studio. Additions are bounded primitive components, not generated neural geometry.

### Real-model evidence

`phase3-assets-v4/live-results.json` records **9/9 passed**: seven completed edit/save/reopen chains and two expected rejections. All used real local Grudge decisions and actual saved GLBs.

| Prompt action | Checked result |
| --- | --- |
| Add red sphere Beacon, diameter 1 m at [3, 0.5, 0] | Existing six-part bench retained; seventh mesh added, 152 active triangles |
| Move Beacon 1 m left | Beacon at [2, 0.5, 0]; other parts retained |
| Remove Beacon | Original six mesh nodes and 72 triangles retained |
| Duplicate Seat | Seventh mesh named Seat copy at +2 m on X |
| Rename Seat copy to Cushion and color only Cushion red | Original Seat remains blue; copy has requested name and red surface |
| Move only Cushion 1 m up | Copy moves; other transforms retained |
| Remove Cushion | Six original parts retained |
| Rename Seat to Backrest | Name conflict rejected before an edit revision |
| Duplicate Seat then remove AbsentPanel | Missing later target rejected in full preflight; history count unchanged |

Seven additional real-model **planning fixtures** pass in `phase3-planner-r14-results.json`: navigate to Local Files, enable local controls, set Style, set a creation prompt without running it, create without Hunyuan, frame a retained model without reopening it, and finish only after the frame status. These controlled snapshots are planner evidence, not native execution evidence. Initial failed structural runs (`v1`, `v3`) and planner logs are retained; they exposed unsupported glTF Node.clone, omitted compound actions, wrong structural schema decoding, renamed-part targeting and context truncation. The corrected run uses an exact tuple schema understood by the installed decoder.

### Native app evidence and corrections

The v20 package completed “Open Local Files.” in one action, then “Create a green cube, make it spin, and save it.” from that page in four actions. The visible green cube has 12 triangles, one embedded surface and a four-second Turntable clip. Actual saved revision `e08f4b56-f25f-4d32-afc1-2cc8fe50da1f` has SHA-256 `08a2978deda0158336c793604f4ed130e6695901c166eaf3b4a3dc41281334bf`. Retained native screenshots: `phase3-navigation-v20.png` and `phase3-green-cube-v20.png`.

The subsequent Forge handoff stopped when toolbar controls remounted or selection changed during inference. Toolbar identity is now stable, semantically identical remounts resolve only when unique, and a changed screen receives up to three fresh plans without replaying the stale action. Frame operations report their result visibly. The style test also exposed Chromium retaining layout rectangles under closed details; closed descendants are now explicitly excluded so Grudge must first open the panel. Editor selection, transform and lighting controls gained exact accessible labels. Invalid numeric/color inputs are checked before dispatch, and applied actions are retained before post-action verification.

The v25 style rerun passes in two actions: opening Optional style and planning settings, then setting Style to low-poly. The open panel, actual Low poly value and both actions were inspected in `phase3-style-visible-v25.jpg`. Forge still attempted to reopen an already handed-off model in v25; the repetition guard stopped it and `phase3-forge-repeated-v25.jpg` retains that failure. The corrected planner binds the current-model handoff and framing result; two real-model fixtures verify that it chooses Frame rather than Open and finishes only with a matching frame status. The final native handoff rerun remains **unverified**: the Windows controller repeatedly returned `failed to activate captured window`, even after refreshing the window, resetting the controller and restarting the task-owned app. Native input stopped at that boundary. This controller failure does not establish an app failure or an app pass.

A separate working copy of the native green cube then completed the real Grudge component prompt through the main creation service. The first attempt was safely rejected because the model incorrectly included Beacon in the existing-target list; the schema now restricts additions to the whole current scene. The corrected run has two meshes, 92 triangles and red Beacon at [3, 0.5, 0], with the original cube surface, texture bytes and complete animation channel data unchanged. Save/reopen hashes match. Revision `85c0ae0f-f394-490b-93e7-35e24667acf7`, SHA-256 `5492f31518e5cbd4c6ede5f6eefe0450d4356ae5cdbd7693063ee47581560fe9`; evidence `phase3-box-service-v2/result.json`. This is actual service/file evidence, not final native UI acceptance. The rejected first attempt remains in `phase3-box-service-v1`.

Four further real-model fixtures pass in `phase3-planner-r19-results.json`: explicitly route a component edit from Forge to Prompt to 3D, open a collapsed disabled-planner setting, enable the existing Grudge planner before running a compound request, and accept numerically equivalent retained field values (1.0 versus 1). There are **11 passing control-planning fixtures total**, separate from the live structural edits and native checks. Numeric/color inputs retain exact semantics without mistaking React formatting for a failed change. Final Forge handoff, editor-field execution and the full app-wide component UI chain remain unverified while native control is unavailable.

### Current verification and boundaries

Source typechecking, production renderer/main/preload/icon build, the app-action, prompt-build, local-creation and Forge animation suites pass. Linear and cubic-spline copy regression checks preserve original motion data. Packaged operational smoke passes for final **v29** (`phase3-smoke-v29.log`). Both TypeScript projects and the full production build pass in `phase3-typecheck-r19.log` and `phase3-build-r19.log`; the latest action suite passes in `phase3-app-actions-r19.log`, and the addition-schema prompt regression passes in `phase3-prompt-build-r18.log`. The renderer-only Grudge libraries are now development dependencies, because Vite bundles them into the renderer; the package no longer incorrectly collects their sibling workspace dependency trees. Missing-dependency/undefined-path collector warnings are gone. Upstream duplicate-reference diagnostics and the child-process shell deprecation warning remain.

- Directory package: `release/prompt-e2e-20260909-v29/win-unpacked/Grudge Dev Tool.exe`.
- EXE SHA-256: `1514CD871C44645808E508A860DB4CF59EF3CCB46F0A9E7BDB72C3BCF9DAA02E`.
- ASAR SHA-256: `342FA534FB64B93FEFE3438C77F51A5451CBCDFC66528B28EFD92E5C5E086A60`.
- Evidence/logs: `.cache/prompt-e2e-20260909/phase3-*`.
- Universal success is not established. Native dialogs, canvas operations, embedded applications, full coding/gameplay/world systems and all connected/admin actions remain outside proven coverage. Hunyuan Paint's earlier free-VRAM failure and additional-provider readiness remain separate; no new successful neural finishing chain is claimed.
- Final test package is launched with the retained local test profile and original green cube history; final native interaction is still blocked.
- Saved main and existing dirty work are preserved. No commit, push, installer, installed-app replacement or public deployment was performed.

## Continuation: compound edits and target accuracy

The expanded campaign exposed silently omitted movement/scaling/animation removal, static rotations mistaken for clips, invented actions after negation, missing brown coloring, incorrect assembly placements, wrong movement axes and substitution of missing numbered parts. These were repaired and rerun through the real installed `grudge-dev:latest` model. No mock planner or manual prescribed build plan was used in the live runs.

The final expanded run is `.cache/prompt-e2e-20260909/phase2-assets-v3/live-results.json`: **13/13 checks passed** (11 actual creation/edit results and two expected failures). Earlier v1/v2 failures remain retained; they are not counted as passes.

| Prompt family | Checked result |
|---|---|
| Brown cube; plain cube with “do not spin”; blue spinning cube | Actual brown texture pixels; no invented texture/clip/library save for the plain cube; real spinning clip and exact saved bytes |
| Move two metres right, rotate 90 degrees about Y, double width, save | Outer placement `[2,0,0]`, Y quaternion for 90°, scale `[2,1,1]`; existing animation remains on the inner asset |
| Remove animation; halve height | No remaining clips; subsequent scale `[2,0.5,1]`; saved/reopened revisions |
| Three-tree world; recolor only Tree 2 crown; move its trunk and crown forward | Seven meshes; only the chosen crown becomes red; both selected parts move +2 on Z while Tree 1 stays put |
| Wooden bench | Four grounded legs, attached seat and backrest; six brown box components |
| Mixed world | Grey ground, exactly two red cubes and one blue sphere, grounded and saved |
| Nonexistent Tree 99; collision physics | Rejected before authoring; no substituted target or partial save |

New edits use the existing immutable creation service. Exact node inventory is supplied to Grudge. Static whole-asset placement sits outside animated nodes so playback cannot overwrite the requested placement. Named transforms stop if an animation owns the same transform. Part colors clone shared material/mesh references as needed, preserving other instances. Removing animation keeps the mesh and hides authored motion-only effects. Scene-state hashes distinguish transform/color/clip changes from the established geometry hash, preserving compatibility with older retained assets.

Literal movement units, directions and scale ratios are bound from each corresponding user clause. Known single-object templates constrain the schema to their existing author and forbid invented assembly components. A follow-up's selected asset kind remains authoritative even when the user names one part inside it. Plans permit up to 12 actions and 64 assembly parts, with one local correction attempt for rejected model output. A persistent error panel keeps failures visible after the toast disappears.

This remains bounded creation/edit coverage. Arbitrary editor commands, topology operations, code/gameplay generation, complex production worlds and the full Hunyuan finishing chain are still outside the demonstrated one-prompt coverage. No additional model download or Hunyuan inference was needed for this continuation.

### Final regression and native evidence

All **nine original examples pass again** against the final planner source: `.cache/prompt-e2e-20260909/phase2-originals-v17.json`. This includes the sword/detail/swipe chain, cosmetic projectile prop, segmented dance, world, arch, cube, sphere, cylinder and torus. Each final original-suite result was saved and reopened with matching hashes. Earlier incomplete reruns and timeouts remain recorded; only task-owned superseded helpers were stopped.

A streamed diagnostic identified the game-prop timeout: the model endlessly enumerated unrelated kinds in `unsupported`. That array and its strings are now bounded, and exact supported-kind/action names are excluded from the unavailable-work list. Genuine unsupported requests still fail the explicit capability checks and plan validation. The corrected real game-prop prompt completed in 10.9 seconds. CPU planning remains variable: the final arch took 141 seconds, including correction work. This is not a latency guarantee.

Normal **v15** controls executed “Move only sphere one metre forward, make only sphere green, and save it.” Build receipt `phase2-assets-v3/prompt-builds/1b3ae5da-f3d8-4b47-8759-d7f65006bf84/build.json` retains two edits and the managed save. Final revision `7afbda17-9e51-4ce6-b549-6077c103120a` has SHA-256 `4ae18708682ec433dbf432ced9a118c07c7d508b37f6de747ab28b5256f5c2e0`. Actual GLB comparison confirms sphere +1 on Z and green `[0,1,0,1]`; other objects and the geometry identity stay unchanged. The native Reopen button replayed this exact revision. `phase2-native-validation.json` and `phase2-native-part-edit-v15.png` retain the checks and screenshot. Native history replay also showed the actual six-part bench (`phase2-native-bench-v15.png`). Restarting the final v17 package restored that bench and its library record.

The final **v17** app then executed “Make only Seat blue and save it.” through its normal prompt control and reopened the saved bench. Receipt `phase2-assets-v3/prompt-builds/82034c15-18da-40d8-86a2-e63b66617f9d/build.json` records the real Grudge plan. Final revision `c30fc163-caed-4297-874b-e67561f85951` has SHA-256 `6f4167d1d29c1e105ab8c9f0f823c51ea0226251dd13992e6d1a9cb7c35ca9ca`. Independent GLB comparison confirms only Seat changes to blue `[0,0,1,1]`; the other five parts and all transforms remain unchanged. The reopened six-part bench was visibly inspected; viewport lighting shifts the displayed hues. Evidence: `phase2-native-seat-validation.json` and `phase2-final-seat-blue-v17.png`. The final test app is left open on this saved result.

Final Windows directory package: `release/prompt-e2e-20260909-v17/win-unpacked/Grudge Dev Tool.exe`.

- EXE SHA-256: `1573668A1A82D13B4935EC4B9C76377E00131C5F708711E6EC2F50D64F78FBFA`.
- ASAR SHA-256: `A66817234449A2989657A1CA6C7E6E9ED2511AF42FBB99AD8216C0A46D3FF043`.
- Passed: final prompt/edit regression suite, local-creation and animation export suites, both TypeScript projects, renderer/main/preload build, icon generation and directory packaging. The sixteen wider suites from the first campaign remain separately recorded below.
- Final package smoke passes in `smoke-v17-retry.log`. The first valid v17 smoke run transiently failed to enter an invalid Seed value (it remained `92`); an unchanged rerun passed. No assertion was weakened. A premature pre-package smoke invocation also failed and is not counted as package evidence.
- Sibling-package dependency collector warnings remain. No installer, release, push or installed-app replacement was performed; changes stay on the existing uncommitted `main`.

The earlier campaign and its separate visual/provider evidence follow below.

The repaired local tool completes the representative creation chains below using the installed `grudge-dev:latest` model. This is **partial coverage of the requested universal prompt interface**. Basic model/component/world blockouts work; arbitrary gameplay, scripts, production worlds and every editor action are not proven from one prompt. Hunyuan geometry ran successfully; Hunyuan Paint was blocked by available GPU memory.

## What was executed

Real local Ollama requests planned the actions; existing CPU geometry, surface, adjustment and animation authors executed them. Nothing was mocked in this campaign. The local model runs on CPU and unloads after planning. No model was downloaded, no cloud generation was substituted, and no release was published.

| Requested creation | Executed result | Evidence boundary |
| --- | --- | --- |
| Blue cube, spin, save | 12 triangles, blue embedded texture, Turntable clip, saved/reopened | Also entered through the packaged UI and visually inspected across frames |
| Curved sword, texture, details, longer blade, swipe, save | 2,950 triangles, retained surface/detail/adjustment revisions and swipe clip | Final package visibly replays the repaired attached inlay and seated blade; saved revision reopened |
| Red cosmetic game prop, projectile, save | 432 triangles, embedded surface and projectile clip | Cosmetic motion; no weapon gameplay or physics claim |
| Segmented person, texture, dance, save | 1,780 triangles and articulated dance clip | Native GLB open and playback inspected across distinct limb/head/leg poses; existing segmented author |
| Green ground with exactly three trees, save | Seven component meshes, 146 triangles, three brown trunks and green crowns | Visible in packaged creation UI and Forge; native scene save/reload passed |
| Stone archway with two pillars and lintel, save | Three connected grey boxes, 36 triangles | Visually inspected; initial disconnected layout was rejected and repaired |
| Green sphere, save | 80 triangles, embedded surface | Real planner and saved-byte verification |
| Blue cylinder, taller, save | 32 triangles, surface and bounded scale change | Real planner and saved-byte verification |
| Red torus, rotate, save | 96 triangles, surface and Turntable clip | Real planner and saved-byte verification |

Exact prompts, local model identity, paths, hashes and the source run for each result are in [consolidated-results.json](../.cache/prompt-e2e-20260909/consolidated-results.json). Earlier failed attempts remain retained. The component/world layout repair used a later rerun; this is not nine untouched first-attempt successes.

A fresh native UI prompt, “Create a blue cube, make it spin, and save it,” completed all four actions and reopened the saved GLB without regeneration. Receipt: `.cache/prompt-e2e-20260909/campaign-assets-v5/prompt-builds/5d6414e3-f8aa-4134-9e32-897b685716b1/build.json`.

The world opened in Forge, saved through the native file dialog, and replaced a temporary two-entity scene when reloaded. The retained `.cache/prompt-e2e-20260909/native-world.forge-scene.json` contains seven embedded geometries and seven materials. Screenshots show the actual world before and after reload. Restarting v10 restored the selected world and saved-library state. Final v11 was opened on the v6 campaign root and restored the corrected sword with its saved-library record and playing clip.

## Optional local enhancement

Real Hunyuan3D 2.1 generated one wooden crate from an actual generated concept. The signed installation and every model-file hash were verified. Job: `8da395b2-9a5b-419f-a485-605792810cd6`, source revision `82920d643c0dc2f7bfd7255f45f62d386edfe60c`.

- GLB: `E:\GrudgePrompt3D\jobs\8da395b2-9a5b-419f-a485-605792810cd6\variant-1\asset.glb`.
- SHA-256: `7afce25137687141ee27604c878f06a96eb72cc6df63c6e0892d44efb8252990`.
- 49,997 triangles; measured height 0.99984 m. Width/depth differ from the requested 1 m under proportional height scaling and are reported by validation.
- Actual front, right, back and left views show one complete crate, broad planks and framing. Some edges are wavy; this is a basic generated prop. Codex performed the inspection and normal approval controls under the owner's testing request; these records are not Al's personal visual acceptance.
- Native “Use existing generated model” followed by “Apply Hunyuan texture” dispatched Paint job `6f533769-a631-4cc0-b99b-fed2d63c92c4`. It stopped at hardware preflight: 19.9 GiB free against a 21 GiB requirement. Paint inference did not run and no painted output is claimed.
- After closing this task's old test windows, a real service retry (`ce4973d9-5f37-44d6-8b19-47943fd906fe`) still had only 20.4 GiB free. The repaired preflight reported the result in 18.4 seconds rather than verifying files for nearly seven minutes first. Other user apps and GPU work were preserved. Integrity checks remain mandatory for a runnable job.
- HY-Motion lacks its required signed installation manifest; TRELLIS lacks required native-artifact verification. Neither is reported ready or exercised as a successful provider.

The complete Hunyuan texture → animation → final-save chain remains unproven in this campaign. Existing workflow, Paint surface-retention, approval and animation suites pass, but they do not replace that live chain.

## Repairs made

1. Repaired duplicate/unfinished merge fragments in local Skeleton access, FBX loading, preview binding and Skeleton Studio; retained existing Play kit routing and exact-source rigging behavior.
2. Restored the declared sibling GrudgeStudioNPM dependency checkout and built its required packages. No duplicate skill tree was created.
3. Connected local prompt planning to bounded execution of multiple existing actions, immutable revisions, managed save and retained build receipts. Familiar affirmative action verbs cannot silently disappear from compound requests. Unsupported actions fail with a capability explanation.
4. Added primitive assemblies for model components and world blockouts. Explicit component counts are schema-bound; basic default trees are grounded and arch parts connected. Dimensions and positions remain bounded.
5. Made Grudge the preferred installed local planning model, including scene-completion planning. A real scene repair plan also returned `grudge-dev:latest`; that plan alone is not a completed world build.
6. Retained prompt context across editor handoff; scene files now embed edited objects/materials/clips instead of losing primitives or reloading only the original source. All entities are prepared before replacing the current scene.
7. Scoped legacy asset-grid styling, corrected clipped/deep model framing in preview and Forge, and bounded neural preview height. History now identifies the original compound prompt, actual model and action position.
8. Reported unavailable Paint hardware before expensive model verification and distinguished hardware measurement from integrity verification in progress text.
9. Native sword inspection exposed a floating decorative inlay and an attachment gap after lengthening. The inlay now derives from the actual current blade surface, is refitted after edits, and blade scaling preserves its seated base. A new real Grudge run (`live-campaign-v6.json`) replaced the rejected sword result in the consolidated report. Geometry checks cover straight/curved blades, length/width/curvature edits and surface alignment.

## Verification and package

Passed: both TypeScript projects; production renderer/main/preload build and icon generation; sixteen focused suites covering creation, prompt execution, animation export, routing, reliability, approval, workflow, Paint retention, recovery, artifact issuance, batch guards, storage, provider cache, viewport navigation and preload. Final camera checks project actual box corners at multiple angles/aspect ratios. Packaged operational smoke passes, and native inspection covers prompt execution, playback, library reopen, Forge handoff, editable scene save/reload and restart recovery.

Final Windows directory package: `release/prompt-e2e-20260909-v11/win-unpacked/Grudge Dev Tool.exe`.

- Executable SHA-256: `86BCB0D9E62DBB684584C230FF1F10A59A589EDD46F40D772E273D71DB424C94`.
- `resources/app.asar` SHA-256: `FDBBA7E6622D3859F128D5ACF93AAC55F147E6D59C1AEA574BE25021F884E05C`.
- Evidence folder: `.cache/prompt-e2e-20260909`; final build/smoke logs end in `v11.log`.
- Working tree on `main`, starting at `94fc8cd`; changes are uncommitted. No installer, installed-app replacement, commit, push or public deployment was requested or performed.
- Packaging still warns about sibling package dependency collection although bundled code builds and tested routes load. These warnings and untested app routes prevent claiming a fully verified release.

## Remaining acceptance

“Any action from a simple prompt” remains broader than the implemented action set. The supported route covers basic geometry/assemblies, bounded edits, procedural surfaces and the listed motions. Arbitrary scripts, interactive gameplay, complex terrain/world systems, arbitrary rigged creatures, additional providers and every editor tool need separate live coverage or implementation. The fleet was unavailable during this local test, so connected catalog/backend deployment behavior was not established.

The next material gate is sufficient free GPU memory for real Paint, followed by visible texture inspection, animation, save and reopen of that same crate. No previous strict six-asset campaign has been marked complete by this run.
